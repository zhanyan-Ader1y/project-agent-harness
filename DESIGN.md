# 设计目标

- 意图确立：进入spec前确立意图，spec下所做的所有变更都不能偏离spec的意图
- review增强：将需要通过对话或者写入到文件中的review内容进行增强，让review的内容更直观、易懂
- 经验沉淀：将vibe以及spec过程中遇到的coding错误记录为经验，让团队共享经验减少重复错误
- 事实准确：仅将架构、代码作为事实，spec、adr等仅供参考

---

# 贯穿四条目标的一条原则

**每条规则都要挪到产生它的那一层去执行。**

写成条款而执行者不在那一层的规则，会被一致地、有记录地绕过。两处实测：

- 前身项目 Themis 的 `实现者不得验证自身产出`，11 次失效 10 次，**十次都在工件里如实写明"身份独立不成立"后照常放行**。修复它的不是把条款写得更明确——前身的复盘结论是「**修复对象是派发层，不是控制面条款**；把派发层问题写成控制面条款是已被证伪的做法」。把要求挪到派发层之后合规 4/4（n=4，无对照）。
- TRACE 论文（arXiv 2606.13174，2026-09-02 读）测出，用 mem0 做记忆层，**57.5% 的适用检查项取到了也照样违反**——存了不等于用了（access-compliance gap）。

Anthropic 官方 SDLC 实践给出同方向结论：**对硬性要求，hook 比建议性的 skill 更合适**。

因此本项目的分工是：**判断放 skill，机械动作放脚本，强制放 hook。** 任何一条规则若只落在 skill 上，必须能说明为什么它不会被跳过。

---

# 呈现形式

**plugin**，仓库同时是 marketplace。

**理由是分发，不是能力**：hook 并非 plugin 专属，项目 `.claude/settings.json` 里也能配。plugin 的真实优势是**一次安装拿到 skills + hooks + MCP 配置 + 脚本**，并可通过 `extraKnownMarketplaces` + `enabledPlugins` 向团队统一推广、统一版本。

---

# 实现思路

## 如何编排superpowers流程

**不造中央流程文件。**

前身项目 Themis 的病因正是这个形状：一份 571 行、每个会话都要全量读的控制面（flow 220 + rules 209 + template 100 + README 42），每个需求产出约 16 份工件、1,200 行。

superpowers 自己已经串好了脊：`brainstorming` → `writing-plans` → `subagent-driven-development`。三份核心工件的产出者都在这条脊上——`brainstorming` 的 architectural 路径产出 spec（其第 6 步即写设计文档），`writing-plans` 产出 plan。**本项目只做插入，不做总谱**：每个 skill 在自己的 `description` 里声明触发时机，由 agent 按需加载。

**工件的顺序由 superpowers 保证，本项目不设计、不接管。** `intent → spec → plan` 这条链是 superpowers 执行链路自带的产物顺序，不是本项目定义的流程——**只做增强，不改动执行链路**。这条边界一旦松动，插入就会退化成总谱。

| 目标 | 挂在哪 | 本项目新增 |
| --- | --- | --- |
| 意图确立 | `brainstorming` 已有审批闸门 | `intent-lock` + 偏离检查（**捆绑交付**，见下） |
| review 增强 | — | `review-presentation` |
| 事实准确 | superpowers `verification-before-completion` | **断言重跑器**（`scripts/assert-replay`） |
| 经验沉淀 | — | `experience-intake` + 写入脚本 + 三条 hook |

工件数向**三**看齐（intent → spec → plan），不向十六看齐。

**四条目标的落地程度并不均衡，如实记下**：目标三（经验沉淀）占本文档一半篇幅且机制齐备；目标四有专属执行者；目标二只需迁移一份现成 skill。**目标一目前一个执行者都没有**——`intent-lock` 与偏离检查捆绑交付，而两者尚未排期。按第一原则的标准，这是四条里最空的一条。

## 事实的优先级

`架构 > 代码` 与目标四的 `仅将架构、代码作为事实` 看似冲突。**冲突源于"架构"一词兼指两类**，拆开后两条都成立，各管一类：

| 要判什么 | 依据 |
| --- | --- |
| **这段代码该不该这么写** | **规范类架构 > 代码**。代码违规时是代码错，所以规范在上 |
| **系统现在到底长什么样** | **代码 > 描述类架构**。描述以代码为正确性来源 |
| 当初为什么这么决定 | ADR。既非应然规则也非实然描述，"仅供参考"是准确定位 |

实证依据：所有严重误诊都是同一形状——**文档或记忆说 X，实跑是 Y**。把任何描述性文档排在代码之上，等于给这类错误发通行证。

### 目标四的执行者：断言重跑器

**这是目标四唯一的机器强制，不是一句原则。**

前身项目对这一族有全项目最强的实测：`docs/plan/spec-replay/hard-enforcement-list.md` 按**实际发生次数**排序的 11 条硬强制清单里，「数值型与存在性 claim 必须由执行时真实运行的命令产生并就地附输出」排第 1 位，实发 8 次以上、全清单最高频，且**机器化成本最低**。其 ADR 进一步判定 `themis verify` 是候选里"唯一能拦住而不只是发现的"。

**同一份记录还排除了一个近似形态**：某次执行者在同一文件紧邻处粘贴了原始输出（11 行），结论里却写了 10。**命令在、输出也在，数字仍然错**——只检查"字段存不存在"覆盖不了"跑了但抄错"。

因此 `scripts/assert-replay`，做**两类**校验：

1. **重跑断言**——输入 `evidence_cmd` + 声称的结果，跑一遍，比对实际输出
2. **符号存在性**——经验中提到的类名、方法名、路径是否**真的存在于代码库**

第 2 类来自腾讯团队的实测案例：系统抽出一条经验称对接 `FastScrollBar` 应调用 `attachToQBListView()`——有场景、有方法名、有操作指引，**纯文本审核大概率放行**；源码检索发现该类根本没有这个方法。其失败模式值得记住：

> Agent 会按指示调用一个不存在的方法，编译失败。**更麻烦的是 Agent 会怀疑自己理解有误，而不是怀疑经验有问题——陷入反复重试的恶性循环。**

用于四处：写入前自检、取回后核实、**定期审计 mem0 库**、**淘汰判定**（见下）。

**没有它，`evidence_cmd` 只是一个没人跑的字符串。**

前身的另一条教训适用于本项目的所有形状检查：**形状约束不阻止绕过，只是让绕过留下可扫描的痕迹——你还得有那个扫描器。** 审计就是扫描器。

## 架构与架构决策记录（ADR）

架构分为**系统架构、代码架构、领域设计架构**。

但真正决定怎么处理的是另一条切分——**架构记录里混着三类性质不同的内容**：

| | 例子 | 过期方式 | 机制 |
| --- | --- | --- | --- |
| **规范类**（应然） | 分层、边界、命名、依赖方向 | **不因代码漂移过期**，但**会因决策被推翻而作废**——而且比描述类更隐蔽，因为不存在"代码与它不符"这个可见信号 | 被新 ADR 推翻时同步标 `supersedes` |
| **描述类**（实然） | 模块清单、依赖关系、领域划分 | 会，且无声 | 采信时以代码为准，见下 |
| **ADR** | 当初为什么这么决定 | **不会**。决策发生过就是发生过，即便后来推翻，记录本身仍准确。append-only | 无 |

**描述类的处置**：不设结构变动提醒 hook（`PostToolUse` 的 matcher 匹配的是工具名，"新增模块"不是工具名；且既然采信时以代码为准，描述陈旧本就不改变任何判断）。

约束是「**描述类架构与代码冲突时，一律以代码为准，且应就地更正描述**」——但**这条不能写在 `architecture-notes` skill 里**：危险时刻是 agent **读**架构描述并采信的那一刻，而该 skill 由 description 触发，只在**写**架构描述时加载。管住了写的人，管不住信的人。

改为一条**路径作用域规则**：

```yaml
# .claude/rules/architecture-description.md
---
paths:
  - "docs/architecture/**"
---
```

官方机制是「路径作用域规则在 Claude 读取匹配文件时才载入」——**执行者与失效点因此对齐**：读到架构描述文件的那一刻，约束自动进上下文，不依赖 agent 记得加载什么。

## 模块级知识记在哪

**不要求每个模块建 `CLAUDE.md` 与 `AGENTS.md`。** 三条理由：

**一、该记的内容比直觉少得多。** `/doctor` 的 trim 规则给了现成判据（<https://code.claude.com/docs/en/memory>，2026-09-02 读）——删掉**能从代码推导的**（目录结构、依赖清单、架构概览），保留**坑、rationale、与工具默认不同的约定**。

「记录模块细节」这个目标本身要收窄：细节大多可从代码推导，写下来就是**制造第二事实源**，正撞目标四。

**二、`AGENTS.md` Claude Code 不读**（"Claude Code reads `CLAUDE.md`, not `AGENTS.md`"，同上出处）。仅当团队确实在用别的 agent 工具时才需要，做法是 `@AGENTS.md` 导入或符号链接——一份内容两个工具读，不并存。注意**导入的文件是启动时加载的**，会抵消按需加载的好处。

*该行为可能随版本变化，本节结论依赖它，须随官方文档复核。*

**三、嵌套 `CLAUDE.md` 成本低但形态不对。** 官方明确子目录下的 `CLAUDE.md` 按需加载，所以常驻成本的担心不成立。但对"只影响一部分代码库"的内容，官方建议 **skill 或 path-scoped rule**：

| | 嵌套 `CLAUDE.md` | `.claude/rules/` + `paths:` |
| --- | --- | --- |
| 按需加载 | ✅ | ✅ |
| 集中可审 | ❌ 散落各模块 | ✅ 一处看全 |
| 边界 | 只能按目录 | 可跨目录，模块边界与目录不一致时仍管用 |
| 冲突 | 多份互相矛盾（官方点名的问题） | 一处可查 |

**分流规则**（属 `architecture-notes` 管辖）：模块级的坑与约定 → `.claude/rules/<module>.md` + `paths:`；多步流程 → skill；**能从代码推导的 → 不写**。

## 经验如何沉淀

**云端 mem0，检索经 MCP，写入经脚本**（端点 `https://mcp.mem0.ai/mcp`，OAuth 或 API key bearer）。

### 写入触发：三条可观测信号唤起 skill

**"记得去记"与"记得去查"结构同构，同样不能只靠 skill 的 description。** 而且"刚犯错正在修"是 agent 最不可能主动加载 skill 的时刻。

因此由 hook 在下列**机器可观测**信号出现时唤起 `experience-intake`：

| 信号 | 挂在 |
| --- | --- |
| 一次 hook deny 或权限拒绝（**须排除本项目自己的闸门**，否则"我写记录被拒了"会自我指涉地构成一条经验） | `PostToolUse` |
| 已通过的测试或 CI 由绿转红 | `PostToolUse` |
| 产生了工作区之外的动作 | `PostToolUse` |
| **本轮用户提示构成一次纠正** | `UserPromptSubmit` |

**第四条不可省**：它是乙分支里最强的信号（TRACE 的 142 条数据全部由此挖出），但它不是工具事件——**用户在对话里做出的纠正，前三条一条也观测不到**。若缺它，这条信号就只在 agent 恰好主动加载 skill 时才用得上，而设立触发机制的全部理由正是不依赖主动加载。

**不增加 hook 数量**：`UserPromptSubmit` 已因检索而存在，纠正判定在同一处顺带完成。

### 写入路径：脚本编排三次独立判定

**产出经验的 agent 不能同时裁决它。**

腾讯团队对同一形状的结论是：「**让 AI 自己判断自己产出经验的价值，等于让裁判同时当运动员——在工程上无效**」，其初版正是"抽取即入库"，**约 90% 是废经验**。这与前身 Themis 的 `实现者不得验证自身产出`（11 次失效 10 次）是同一结构问题的两次独立观察。

解法**不是加人工审批**，而是把治理拆成**各自独立上下文的三次判定**，由 `scripts/experience-write` 编排：

| 层 | 目标 | **默认方向** | 依据 |
| --- | --- | --- | --- |
| **Review** | 拦明确垃圾 + 事实性校验 | **默认保留，定向过滤** | 九类垃圾特征 + `assert-replay` |
| **Dedup** | 候选集内去重 | **宁严勿宽** | 否决式规则，见下 |
| **Merge** | 与历史库的关系 | **保护历史边界** | create / update / skip / contradict |

**三层的默认方向必须不同**：漏掉一条好经验的代价大于放行两条边缘经验（故 Review 默认保留）；**误合并造成的边界污染不可逆**（故 Dedup 宁严勿宽）；已入库的是已验证资产，新经验要跨更高门槛才能改它（故 Merge 保护历史）。**"宁可错杀"和"宁可放过"的方向在各层完全相反，用一个 prompt 一次判完，各层目标会互相牵制。**

三层的判据分别存放在 `skills/experience-intake/{review,dedup,merge}.md`，脚本各起一次判定、只喂对应那一份——**`SKILL.md` 本身保持短**。

完整流程：**抽取 → Review → Dedup → Merge → 红线过滤 → 带 `infer=False` 写入 REST API**。

同时 hook **deny 裸调用 mem0 的写入与删除工具**。写入必须走脚本，四个理由：

1. **查库变成不可跳过的机器动作。** Merge 层要求"写入前先查历史库"——若这一步留在 skill 里，跳过它的后果很具体：同一错误堆成 N 条互不相识的候选，**永远到不了第二次，重复检测静默死亡且无处记录**。
2. 红线过滤有地方放（见下）。
3. **前提假设 1 与 5 的退路都落在这里**：假设 1 不成立时改走 REST 并带 `infer=False`；假设 5 要求写入失败必须硬失败、不得静默丢弃——两者都是脚本的行为，不是 hook 的。
4. 省掉一条独立的形状检查 hook——脚本自己就是校验点。

### 抽取判据

**判定一条信息是不是经验，标准只有一个：被召回后，Agent 能否产生正向的行为变更。**

由它推出两道判据，**顺序不可颠倒**。

#### 第一道：可发现性——三镜头（必要条件）

> **如果 Agent 自己就能推出来，召回它没有任何额外价值。**

按**认知障碍**分类，不按主题或重要性——那些维度要么列不完，要么依赖主观判断；**"Agent 能不能自己发现"是客观可判定的**。

| 镜头 | 障碍本质 | 例 |
| --- | --- | --- |
| **黑话** | 语义不可发现 | 项目内部的缩写、代称，字面完全无法推导 |
| **索引** | 位置不可发现 | 某能力入口不在直觉路径上，读代码找不到，需要知道"去哪找" |
| **逻辑** | 行为不可发现 | 反直觉的工程约束、隐式机制，常规推理推不出来 |

**三镜头都不命中即不记录**，无论它多高频、代价多大。

#### 第二道：值不值得占位

三镜头命中之后，再判它是否够格进入检索：

**甲——重复出现**（在 Merge 层判定）。官方实践的原话是"Claude 犯错两次，更正就写进 `CLAUDE.md`"，但**那条规则的执行者是人**。本设计的写入路径里没有人，agent 跨会话、跨成员都无从知道这是第几次。因此把"是不是第二次"**从记忆问题改成查询问题**，由脚本强制执行：查不到 → `status: candidate`，不进入检索；查得到 → `occurrences + 1`，翻为 `confirmed`。

**并发竞态须由脚本处理**：A、B 同时首次撞上同一错误，各自查不到、各写一条候选，此后两条互为"第一次"、**永不晋级**。脚本在写入前须做一次候选归并，否则跨成员检测——候选池存在的唯一理由——失效。

**乙——单次高代价。每条信号必须绑一个写入方无法自由撰写的锚，取不到锚即不许走乙分支，退回存候选。**

| 信号 | 必附的锚 |
| --- | --- |
| 人纠正过 | 用户原话**逐字引用** + 轮次定位 |
| 同一 `evidence_cmd` 连续失败 ≥ 3 次 | 三次的命令与退出码 |
| 产生了工作区之外的影响 | 具体动作与目标 |
| 被闸门拒绝过 | 被拒的 hook 名与时间（排除本项目自身闸门） |
| 弄坏了已通过的测试或 CI | run id 或命令退出码 |

**锚是必需的，因为形状检查只能验证"字段存在且取值在枚举内"，验证不了它为真。** 前身实测过精确的反例：详细设计把 Agent 自己的推论包装成人类评审者的要求，**引用格式完全合规，只是批复原话里根本没有那句**，静态检查与人工通读都放得过去。其结论是：**凡声称某项决定来自人类，都必须可追溯到逐字原话；追溯不到即拦。**

#### 每条经验必须自证

产出时同时给出三项，否则 Review 层拒收：

- **为什么这是经验**——命中哪个镜头、解决了什么可复用问题
- **通过了哪些垃圾检查**——见下九类
- **证据是什么**——哪几轮对话、哪条命令的输出支撑它

### Review 层：九类垃圾特征

**先研究垃圾，再反推好经验**——"好经验"难一次定义到位，"垃圾"的特征更容易归纳。这九类是腾讯团队把垃圾率从 90% 打到 5% 的起点，直接采用：

| 特征 | 说明 |
| --- | --- |
| 事实性错误 | 编造不存在的约束，把对话里的临时说法当规则 |
| 通用常识 | 任何项目都适用，不体现团队特异性 |
| 对话摘要 | 只复述过程，没有沉淀成可复用判断 |
| 一次性 case | 只对当前任务有效，不能迁移 |
| 用户主观偏好 | 个人选择，不代表团队规范 |
| 缺少上下文 | 不知道对应哪个模块/组件/接口 |
| 粒度混用 | 一条里兼顾通用规则和 case 特定信息 |
| 不可执行 | 只有抽象建议，Agent 看完不知道怎么做 |
| 证据不足 | 对话里没有足够依据，模型自己推断过多 |

「事实性错误」一类**不靠文本判断**，交 `assert-replay` 做符号存在性与命令重跑校验。

### Dedup 层：否决式规则，默认回退新建

**语义相似不等于经验等价。经验的边界在于适用场景的约束条件。**

以下任一成立即**不得合并**：

- **When 不同不能合**——即使结论看起来相似，场景不同就是两条
- **主结论类型不同不能合**——操作建议 / 风险规避 / 排查方法不能混为一谈
- **局部子机制与完整系统经验不能合**——粒度不一致，不能互相替代
- **同一技术事实但用途不同不能合**
- **★ 禁止桥接式合并**——A 与 B 有重叠、B 与 C 相关，**不能据此推断 A 与 C 重复**

> 一旦桥接式合并把边界不同的经验串联归并，**边界信息就永久丢失了**。

**判不准就新建。** 早先版本用的「语义相似度 ≥ 0.85 且至少一个结构锚重合」已废弃——结构锚重合恰恰可能成为桥接的那座桥。

参照数据：他们的 Dedup 整体 F1 71.79%，严格重复（What 与 When 均同）Recall 91.67%，但 **batch 间波动明显**（某批 6 个正例只识别出 1 个）。**说明这一层对输入分布敏感。**

### Merge 层：唯一目标前置，冲突不自动决断

四种动作：

| 动作 | 何时 |
| --- | --- |
| `create` | 库中不存在同类目标 |
| `update` | 与历史同向，且带实质新信息 |
| `skip` | 核心结论可互相替代 |
| **`contradict`** | **核心结论直接冲突——标记，不自动取舍** |

- **唯一目标判定前置**：先召回相关子集，判断是否存在"唯一最合适的目标"。**没有就默认回退 `create`**——避免把一条不够匹配的历史经验硬推成 update 目标而污染边界。
- **`update` 必须自检**：合并后 When 是否过度泛化（丢了场景约束）、What 是否保留了最具体最可执行的建议、Why 是否保留核心机制、是否引入了原经验不支持的新结论。参照数据：他们的 update **Recall 96.30% 但 Precision 仅 78.79%——模型有明显的"积极合并"倾向**，这是最需要防的一类。
- **`contradict` 不自动放过，也不自动决断**：新旧认知不一致**本身就是有价值的信号**，不应被自动压制。标记后进人工裁决队列。

**`contradict` 是唯一进人工的路径**，且它不是准入审批——它处理的是两条已入库经验的冲突，不改变"skill 是准入唯一裁决者"。

### 条目结构

经验本身不是事实，但必须携带指向事实的指针：

```text
information:  一句话——什么做法导致什么错误
metadata:
  repo, commit      发生时的代码状态
  evidence_cmd      能复现或证伪它的命令（由 assert-replay 实际重跑）
  evidence_digest   退出码 + 输出摘要（不落原文，见红线）
  files             仓库相对路径
  author, at        谁写的（mem0 侧无归属，必须自带，故不可信，仅供线索）
  lens              blackword | index | logic —— 命中哪个镜头，缺失即拒收
  status            candidate | confirmed | contradicted | retracted | stale
  occurrences       出现次数
  promoted_from     走甲分支时：被撞上的那条候选的 memory_id
  contradicts       走 contradict 时：与之冲突的 memory_id
  high_cost         走乙分支时：五条信号中的哪一条
  high_cost_anchor  该信号对应的锚（逐字原话 / 退出码 / run id 等）
  recalled_n        被召回次数        ┐ 淘汰判据，
  adopted_n         被采纳次数        ┘ 由召回旁路回填
  last_checked      上次通过 assert-replay 的时间
```

**只有 `status: confirmed` 进入检索。** `contradicted` 与 `stale` 暂停召回但保留，`retracted` 永久排除。

### 红线与撤回

**红线过滤（写入脚本内强制）**：`evidence_output` **不落原文**，只留退出码与摘要；`files` 只留仓库相对路径。

理由：命令原始输出可能含密钥、内网地址、客户数据或私有代码，一旦推到第三方云服务**就拿不回来**。

**撤回路径**：经同一个写入脚本写 `status: retracted`，检索排除。deny 只针对 agent 的裸删除工具。**撤回不是人工审批，不违反"skill 是唯一裁决者"。**

没有撤回路径的话，一条错误的 `confirmed` 会被每轮注入，而它不能删（hook deny）、不能评审（无 diff）、不能归责（`author` 自填）。

### 淘汰：挂在定期库审计上，三条判据

腾讯团队把这条列为三个未解方向之一：**长期未命中、未采纳的经验没有自动淘汰机制，库会随时间静默膨胀**；项目演进时（模块重构、能力下线）历史经验可能**集体失效**，且没有识别"需要复核"的能力。

> 生产决定基线 → 使用暴露问题 → 维护反哺生产。**任一环不闭合，经验系统就会从资产退回为噪声。**

**本项目的失效范围比他们更大**——经验存云端、跨仓库共享，一次模块重构可能让另一个仓库里的召回集体失准。

淘汰不新增组件，挂在已有的**定期库审计**上（`scripts/assert-replay`），三条判据：

| 触发 | 处置 |
| --- | --- |
| `evidence_cmd` 跑不通，或 `files` 已不存在 | 标 `stale`，暂停召回，进复核队列 |
| `recalled_n ≥ 10` 且 `adopted_n = 0` | 标 `stale`——召回了从不被采纳，等同噪声 |
| `confirmed` 但超过 N 个月未被 `assert-replay` 校验过 | 强制重校验 |

第二条依赖采纳计数，而采纳计数依赖下面那个尚未解决的观测问题。

### 检索：`UserPromptSubmit`，带双上限

**不挂 `SessionStart`**——那一刻用户还没提问，`search_memories` 没有查询词，只能注入全部（= 571 行控制面的云端版）或注入无关内容；且它一次性触发，会话中途换话题就没有第二次检索。

挂 `UserPromptSubmit`：query 就是用户这一轮的提示，天然有上下文，每轮都触发。

**注入须设双上限**（条数 + token），超出即截断，并**计入统一的上下文预算**。

**校验在注入前完成，不是让 agent 自己去跑。**

"取回后请先跑 `evidence_cmd` 再采信"是一条对 agent 的要求，没有执行者——正是 TRACE 那 57.5% 的形状。改为**注入前的机器动作**，由检索脚本执行：

1. 检索命中若干条 → 逐条查 `last_checked`
2. 过期的条目先跑一次 `scripts/assert-replay`
3. **跑不通的标 `stale`，不注入**；跑通的更新 `last_checked` 后注入

超时兜底：单条校验超时按跑不通处理（不注入），避免校验拖慢每一轮提示。

仍随附的使用约束只剩一句——**与当前代码冲突时以代码为准**——它不要求 agent 主动做什么，只在冲突发生时给出优先级。

### 用途限定与它的收益上限

**只作检索参考**，不编译成必过检查。

**但要承认这条路的天花板**：TRACE 数据（57.5% 取到了也照样违反）量的正是"检索参考"这条路。**目标三的预期收益上限被这个数标在四成左右。**

腾讯团队跑通之后给出同一个观察，措辞更直接：**"命中不等于采纳——目前缺乏观测 Agent 是否真的依据经验改变行为的手段。"** 他们的召回率已达 68.8%，但采纳率无从得知。

因此必须度量：**经验被检索出来之后，有多少比例真的改变了行为**——这是目标三唯一有意义的指标，也是上面淘汰判据里 `adopted_n` 的来源。**这个观测手段目前无人解决，本项目也没有方案**，先记为已知缺口；在它落地前，淘汰只能靠第一、三条判据。

**升级路径（记下，当前不做）**：官方实践的终点不是记忆条目，是**每次事故变成一条永久 eval**。架构上不冲突，读的还是同一个库。

### 选云端 mem0 放弃了什么

跨仓库、跨工具、跨机器共享是本目标的硬要求，仓库内 `CLAUDE.md` 做不到。代价是真实的：

| | 仓库内 `CLAUDE.md` | 云端 mem0 |
| --- | --- | --- |
| 共享范围 | 单仓库 | 跨仓库/工具/机器 |
| 可评审 | ✅ 走 PR、可 diff、可归责 | ❌ 无 diff，`author` 自填 |
| 逐字保留 | ✅ 就是文件 | ⚠️ 依赖 `infer=False`（前提假设 1） |
| 数据边界 | 不出仓库 | 出到第三方，故需红线过滤 |
| 上下文成本 | 每会话全量读 | 按需检索 |

---

# SKILL 清单

| skill | 管什么 | 来源 |
| --- | --- | --- |
| `experience-intake` | 经验准入的唯一裁决者。`SKILL.md` 保持短，三层判据分置 `review.md` / `dedup.md` / `merge.md`，由写入脚本各起一次独立判定、只喂对应那份 | 新写 |
| `review-presentation` | review 内容如何呈现给人 | **迁移** Themis 现成的八条规则（来自 12 份真实投影的实测） |
| `architecture-notes` | 规范类与描述类架构的写法与分流；含"与代码冲突时以代码为准" | 新写，`< 100 行` |
| `intent-lock` | 把 `brainstorming` 产出的散文式设计收成可机械比对的意图锚 | 新写，**与偏离检查捆绑** |

**只有这四个。** 另有三条明确不设：

- **不设 `adr-write`**：「删掉会发生什么坏事」答不出——目标四把 ADR 定为"仅供参考"，全文没有任何机制读它、检索它、校验它。需要时一句话并入 `architecture-notes`。
- **规范类与描述类架构不拆成两个 skill**：拆开有**触发死锁**——agent 要先判断内容属规范还是描述才知道加载哪个，而这个判断正是 skill 的内容本身；两个 description 还会在"架构"话题上互相竞争，实际多半两个都加载。
- **不设 `experience-recall`**：检索触发交给 hook，剩下的"取回来怎么用"是三句话，随注入内容一起给出。

`review-presentation` 需补上官方 `REVIEW.md` 的另一半——**「吹毛求疵的数量上限」与「什么不该报」**（生成文件、CI 已覆盖的检查）。现有八条只讲了"怎么呈现"。

**`intent-lock` 必须与偏离检查捆绑交付。** 单独做它净收益为零：`brainstorming` 本来就产出意图，`intent-lock` 的全部增量在"可机械比对"这四个字上，而比对者不存在时这四个字不兑现。**要么一起做，要么一起推后。**

# hook 清单

| 事件 | matcher | 作用 | 失效后果 |
| --- | --- | --- | --- |
| `UserPromptSubmit` | — | ① 按本轮提示检索 mem0，注入前先校验、带双上限；② 判定本轮提示是否构成一次纠正，命中则唤起 `experience-intake` | 经验取不到；且用户纠正无人观测 |
| `PreToolUse` | `mcp__plugin_experience_mem0__(add_memory\|update_memory\|delete).*` | 一律 deny，写入只能走脚本 | 查库可被跳过，甲分支静默死亡；库可被清空 |
| `PostToolUse` / `Stop` | 三条可观测信号 | 唤起 `experience-intake` | **库长期为空且无人知晓** |
| 待与 `intent-lock` 同批 | — | 意图偏离检查 | 目标一无执行者 |

**意图偏离检查的两种形态**，取前提假设 4 的结果：

- `type: "agent"` 可用 → 子代理读意图锚 + 本次 diff，返回 allow/deny
- 不可用 → 降级 `type: "prompt"`，挂 `Stop`，仅比对意图锚文本与本轮变更摘要，**结果为提醒不为 deny**（降级后不再声称是强制）

**matcher 有两个会静默失效的陷阱，都不报错：**

1. **前缀形态**：插件提供的 MCP server，工具名是 `mcp__plugin_<plugin-name>_<server-name>__<tool>`，**不是** `mcp__<server-name>__<tool>`。本项目 plugin 名 `experience`、server 名 `mem0`，因此前缀为 `mcp__plugin_experience_mem0__`。**写成 `mcp__mem0__` 匹配不到任何工具。**
2. **精确匹配**：只含字母数字与 `_ - , |` 的匹配值按**精确字符串**比较。所以末尾的 `.*` 或括号是必需的——没有正则字符，整条就退化成精确串。

两者叠加意味着一个看起来合理的 matcher 可以完全不生效而毫无提示。**改动 plugin 名或 server 名时必须同步改 matcher**，并由 eval 用例守住。

**hook 脚本不得依赖 `jq`**（本机未安装，官方示例几乎全用它，会静默失败），路径一律用 `${CLAUDE_PLUGIN_ROOT}`。

# 脚本清单

| 脚本 | 作用 |
| --- | --- |
| `scripts/experience-write` | 查库 → 交 skill 判定 → 红线过滤 → `infer=False` 写入；候选合并 |
| `scripts/assert-replay` | 重跑 `evidence_cmd` 并比对 + 校验符号存在性；用于写入自检、注入前校验、库审计、淘汰判定 |

---

# 仓库结构

```text
project-agent-harness/
├── .claude-plugin/marketplace.json     # source 相对本目录的父目录解析
├── plugins/experience/
│   ├── .claude-plugin/plugin.json      # 只有这个文件放这里
│   ├── .mcp.json                       # 云端 mem0（仅检索）
│   ├── hooks/hooks.json
│   ├── scripts/                        # experience-write / assert-replay
│   ├── skills/
│   │   └── experience-intake/
│   │       ├── SKILL.md                # 短；抽取判据 + 指向下面三份
│   │       ├── review.md               # 九类垃圾，默认保留
│   │       ├── dedup.md                # 否决式规则，宁严勿宽
│   │       └── merge.md                # 四动作，保护历史边界
│   └── README.md                       # 模块核心设计随包走
├── evals/
├── DESIGN.md
└── docs/
    ├── decisions/                      # ADR
    ├── references/                     # 外部参考的提炼
    └── review/                         # 评审发现与逐条处置追踪
```

`commands/`、`agents/`、`skills/`、`hooks/` 必须在 plugin 根目录，**不能放进 `.claude-plugin/`**。开发期用 `claude --plugin-dir ./plugins/experience`，改完 `/reload-plugins`，提交前 `claude plugin validate`。

---

# 约束

## 上下文预算

前身的失败是 571 行常驻控制面。本项目：

- **常驻成本 ≤ 400 tokens**：全部 skill 的 `description` 之和 + hook 配置
- **每轮注入 ≤ 1,500 tokens 且 ≤ 5 条**：经验条目 + 使用约束，超出即截断

**两条分列，因为它们的增长方式不同**：前者固定，后者随库增长——**只框住前者等于框住了不会长的那部分。**

- 单个 SKILL.md ≤ 250 行
- `claude plugin details <name>` 核组件清单与预估 token 成本
- `InstructionsLoaded` hook 核**实际**加载了哪些指令文件、何时、为何

以上为初始值，须以实测修正。**"实测修正"不是超标的豁免**：改动阈值必须同时改动本节数字并说明依据。

## 维护面预算

**常驻上下文轻，不等于维护面轻。** 当前的账：4 skill（其中 `experience-intake` 带三份判据文件）+ 4 hook + 2 脚本 + 17 字段 schema + `.claude/rules/` 分流规则 + 5 条前提假设。**"会不会再变庞大"该量的是这个总和**，不只是 400 tokens。

三层治理是本项目唯一一次**主动增重**，理由是有实测支撑：不分层的"抽取即入库"实测约 90% 是废经验，而分层后降到约 5%。**这是全文档唯一有前后对照数据的增项。**

新增任何一项前，先回答"删掉它会发生什么具体的坏事"。答不出的不加。

## eval 套件

- **谁跑**：CI，触发条件为 `skills/`、`hooks/`、`scripts/` 任一变更
- **阈值**：冒烟集必须 **100%** 通过，方可合入；其余用例先只记录不卡门
- **不达标怎样**：拒绝合入该变更

**冷启动不等真实任务**——第一批种子直接取自本文档已识别的静默失效模式，今天就能写：

1. matcher 写成 `mcp__mem0`（精确匹配）时闸门必须被检出为未生效
2. 缺字段的 `add_memory` 必须被 deny
3. 裸 `delete_all_memories` 必须被 deny
4. mem0 不可达时的降级行为符合预期（见前提假设表）
5. 注入超过双上限时必须截断

**经验质量必须三个指标一起看**，只看其一必然跑偏——**只看 Precision 模型会过度保守，只看 Recall 垃圾会变多**：

| 指标 | 量什么 |
| --- | --- |
| **Recall** | 该提取的提了多少 |
| **Precision** | 提取的有多少不是垃圾 |
| **Garbage Rate** | 不该提取的提了多少 |

Dedup 层须单独测——参照数据显示它**对输入分布敏感、batch 间波动明显**。

**Prompt 调优走固定流程，不凭感觉**：固定样本 → 当前 prompt 提取 → 与人工标注对齐 → 分析错例 → 抽象成规则 → 重新评测 → 对比指标变化。固定评测集防过拟合。

后续补入：目标三的核心指标——**经验被检索出来之后，有多少比例真的改变了行为**（观测手段未解，见上）。

---

# 前提假设与退路

以下五条**按可用推进，均未实测**；假设一旦不成立，失效方式都是静默的。

| # | 假设 | 若不成立 | 退路 |
| --- | --- | --- | --- |
| 1 | REST `infer=False` 能让 `metadata` 原样保留 | `evidence_cmd` 失真，经验退化为不可证伪的说法 | 证据移出 mem0，另置只增不改的存储；mem0 只存指针 |
| 2 | mem0 的自动 ADD/UPDATE/DELETE 不吃掉 `occurrences` 与 `status` | **甲分支整体失效**，「重复出现」无从判断 | 计数移出 mem0；或改为纯乙分支 |
| 3 | `search_memories` 支持按 `status` 过滤 | **候选池全量涌进注入**——未审内容 + 无差别检索，最坏组合 | 候选与确认分两个 mem0 scope，检索只连确认那个 |
| 4 | `type: "agent"` hook 可用（官方标 experimental） | 意图偏离检查没有强制形态 | 降级 `type: "prompt"` 挂 `Stop`，只提醒不 deny |
| 5 | mem0 不可达时 hook 可静默降级而不阻断会话 | 网络抖动即阻断工作，或闸门与检索一起无声消失 | 检索失败静默跳过并在 transcript 留一行；**写入失败必须硬失败**，不得静默丢弃 |

**假设 1、2、3 同源**——都取决于 mem0 写入与检索路径上那次 LLM 处理，**一次写入 + 一次检索可同时证伪**。三条都塌的话，mem0 退化为纯指针存储，证据另置。

# 阻塞目标本身的未知

**`user_id` 如何确定**，官方 MCP 文档未说明。这不是推广节奏问题：

- 若按 `user_id` 隔离且每人一个 → **跨成员重复检测与团队共享同时不成立**，目标三失去大半
- 若共用一个 → `author` 是纯自填字段，归属不可信

**必须在实现前查清。**

**受管环境的 `allowManagedHooksOnly`** 可能整体禁用本项目的 hook，闸门静默失效——团队推广前须确认。

**凭据分发未定**：`.mcp.json` 里的 OAuth / API key 如何发到团队每个人手上、放在哪，是推广的硬前置，尚无方案。
