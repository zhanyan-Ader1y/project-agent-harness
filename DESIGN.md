# 设计目标

- 意图确立：进入spec前确立意图，spec下所做的所有变更都不能偏离spec的意图
- review增强：将需要通过对话或者写入到文件中的review内容进行增强，让review的内容更直观、易懂
- 经验沉淀：将vibe以及spec过程中遇到的coding错误记录为经验，让团队共享经验减少重复错误
- 事实准确：仅将架构、代码作为事实，spec、adr等仅供参考

---

# 贯穿四条目标的一条原则

**每条规则都要挪到产生它的那一层去执行。**

写成条款而执行者不在那一层的规则，会被一致地、有记录地绕过。两处实测：

- 前身项目 Themis 的 `实现者不得验证自身产出`，11 次失效 10 次，**十次都在工件里如实写明"身份独立不成立"后照常放行**。修复它的不是把条款写得更明确——前身的复盘结论是「**修复对象是派发层，不是控制面条款**；把派发层问题写成控制面条款是已被证伪的做法」。把要求挪到派发层之后合规 4/4（n=4，无对照）。
- TRACE 论文（arXiv 2606.13174，2026-09-02 读）测出，用 mem0 做记忆层，**57.5% 的适用检查项取到了也照样违反**——存了不等于用了（access-compliance gap）。**该数的口径未本地记录**（分母如何定义"适用"、哪个任务集、mem0 的哪种配置均不可查，且 ClawArena 与 MemoryArena 数字相差很大），裁定见 `docs/review/` U4。

Anthropic 官方 SDLC 实践给出同方向结论：**对硬性要求，hook 比建议性的 skill 更合适**。

因此本项目的分工是：**判断放 skill，机械动作放脚本，强制放 hook。** 任何一条规则若只落在 skill 上，必须能说明为什么它不会被跳过。

---

# 呈现形式

**plugin**，仓库同时是 marketplace。

**理由是分发，不是能力**：hook 并非 plugin 专属，项目 `.claude/settings.json` 里也能配。plugin 的真实优势是**一次安装拿到 skills + hooks + MCP 配置 + 脚本**，并可通过 `extraKnownMarketplaces` + `enabledPlugins` 向团队统一推广、统一版本。

---

# 本文档的范围——最小可用发布

**本文档现在只写最小可用发布所必需的设计。其余已作出的设计决定移入 [`docs/deferred.md`](docs/deferred.md)，逐条带原文、理由与拿回条件。**

最小可用的定义是一句话：

> **一条经验能被写进跨会话、跨成员共享的库；下一次相关提问时，它自动回到上下文里，且注入前机器验过它声称的符号真的存在。**

这是「AI 每次进入项目都从零开始」的最小反面。分档判据只有一条：**不做它，这句话不成立**的留在本文档；做了更好、不做这句话照样成立的，移入 `docs/deferred.md`。

**移出的不是废案，是排期。** 拿回时原样搬回本文档对应位置。

按此分档，本文档覆盖**目标三（经验沉淀）**与**目标四的两个消费方**（写入自检、注入前的符号校验）；**目标一（意图确立）与目标二（review 增强）整条移入 `docs/deferred.md`**——这是分档里代价最清楚的一笔，见该文件对应两节。

`Intent.md` 第四节的四条不可让渡约束没有任何一条被移出：约束 1 见「上下文预算」「维护面预算」，约束 2 见下节「不造中央流程文件」，约束 3 见「条目结构」与「用途限定」，约束 4 的执行者是 Review 层与 deny hook，两者都在本文档里。

---

# 实现思路

## 如何编排superpowers流程

**不造中央流程文件。**

前身项目 Themis 的病因正是这个形状：一份 571 行、每个会话都要全量读的控制面（flow 220 + rules 209 + template 100 + README 42），每个需求产出约 16 份工件、1,200 行。

superpowers 自己已经串好了脊：`brainstorming` → `writing-plans` → `subagent-driven-development`。三份核心工件的产出者都在这条脊上——`brainstorming` 的 architectural 路径产出 spec（其第 6 步即写设计文档），`writing-plans` 产出 plan。**本项目只做插入，不做总谱**：每个 skill 在自己的 `description` 里声明触发时机，由 agent 按需加载。

**工件的顺序由 superpowers 保证，本项目不设计、不接管。** `intent → spec → plan` 这条链是 superpowers 执行链路自带的产物顺序，不是本项目定义的流程——**只做增强，不改动执行链路**。这条边界一旦松动，插入就会退化成总谱。

| 目标 | 挂在哪 | 本项目新增 | 分档 |
| --- | --- | --- | --- |
| 意图确立 | `brainstorming` 已有审批闸门 | `intent-lock` + 偏离检查（**捆绑交付**） | **整条延后**，见 `docs/deferred.md` |
| review 增强 | — | `review-presentation` | **整条延后**，见 `docs/deferred.md` |
| 事实准确 | superpowers `verification-before-completion` | **断言重跑器**（`scripts/assert-replay`） | 四个消费方留两个，见下 |
| 经验沉淀 | — | `experience-intake` + 写入脚本 + 检索 hook | 最小可用的主体 |

工件数向**三**看齐（intent → spec → plan），不向十六看齐。

**四条目标的落地程度并不均衡，如实记下**：目标三（经验沉淀）占本文档一半篇幅且机制齐备；目标四有专属执行者；目标二只需迁移一份现成 skill。**目标一目前一个执行者都没有**——`intent-lock` 与偏离检查捆绑交付，而两者尚未排期。按第一原则的标准，这是四条里最空的一条。

## 事实的优先级

`架构 > 代码` 与目标四的 `仅将架构、代码作为事实` 看似冲突。**冲突源于"架构"一词兼指两类**，拆开后两条都成立，各管一类：

| 要判什么 | 依据 |
| --- | --- |
| **这段代码该不该这么写** | **规范类架构 > 代码**。代码违规时是代码错，所以规范在上 |
| **系统现在到底长什么样** | **代码 > 描述类架构**。描述以代码为正确性来源 |
| 当初为什么这么决定 | ADR。既非应然规则也非实然描述，"仅供参考"是准确定位 |

实证依据：所有严重误诊都是同一形状——**文档或记忆说 X，实跑是 Y**。把任何描述性文档排在代码之上，等于给这类错误发通行证。

### 这三条的执行者：读的那一刻（`hooks/fact-priority.js`）

**危险时刻不是有人写文档的时候，是有人读了文档并采信它的时候。** 因此这条规则挂 `PostToolUse` 的 `Read`：文档内容刚进上下文，优先级约束紧跟着进去，不依赖 agent 记得加载什么 skill。

三类分开说，因为**它们的过期方式不同**：

| 命中 | 注入的约束 |
| --- | --- |
| 路径含 `architecture` / `架构` | 描述系统现在长什么样，正确性来源是代码；**冲突时以代码为准，并就地更正描述** |
| 路径含 `spec` 段，或 `-design.md` 结尾 | 当初的意图与方案，**仅供参考，不是当前事实** |
| 路径含 `adr` / `decisions` 段 | 记录当初为什么这么决定，append-only；**即便决策被推翻，记录本身仍准确——但它不描述现状** |

**只认 Markdown，且路径段要整段匹配。** 这条限制是承重的：否则 `foo.spec.ts`、`lib/architecture/graph.rs` 会被命中，而**那是把代码降级成参考材料，正好把目标四反过来执行**。去掉它，`evals/fact-priority.test.js` 里三条立刻失败。

**原设计的执行者不成立，如实记下**：`docs/deferred.md` 选的是 `.claude/rules/` 路径作用域规则，理由同样是"读到匹配文件时才载入"。**但插件不能提供 rules**（2026-09-04 查官方文档确认：插件组件类型里没有它，`.claude/rules/` 是项目级功能）。失效点没变，执行者换成插件带得走的那个。

**能力边界**：这是**注入，不是拦截**——agent 仍然可以不照做。目标四唯一的**硬**强制仍然只有 `assert-replay`（真去跑那条命令、真去查那个符号）。原设计选路径作用域规则时同样是注入，这一点没变差；它值得存在的理由是**时机**：一句写在 skill 里的"以代码为准"，只在 agent 恰好加载了那份 skill 时才在场。

### 目标四的执行者：断言重跑器

**这是目标四唯一的机器强制，不是一句原则。**

前身项目对这一族有全项目最强的实测：**前身仓库**的 `docs/plan/spec-replay/hard-enforcement-list.md`（**不在本仓库**；本地提炼件见 [`docs/references/themis-hard-enforcement-list.md`](docs/references/themis-hard-enforcement-list.md)）按**实际发生次数**排序的 11 条硬强制清单里，「数值型与存在性 claim 必须由执行时真实运行的命令产生并就地附输出」排第 1 位，实发 8 次以上、全清单最高频，且**机器化成本最低**。其 ADR 进一步判定 `themis verify` 是候选里"唯一能拦住而不只是发现的"。

**同一份记录还排除了一个近似形态**：某次执行者在同一文件紧邻处粘贴了原始输出（11 行），结论里却写了 10。**命令在、输出也在，数字仍然错**——只检查"字段存不存在"覆盖不了"跑了但抄错"。

因此 `scripts/assert-replay.js`，做**两类**校验：

1. **重跑断言**——按 `evidence_cmd` 实际执行，比对声称的退出码与输出特征
2. **符号存在性**——经验声称存在的文件与标识符，是否真的在代码库里

第 2 类来自腾讯团队的实测案例：系统抽出一条经验称对接 `FastScrollBar` 应调用 `attachToQBListView()`——有场景、有方法名、有操作指引，**纯文本审核大概率放行**；源码检索发现该类根本没有这个方法。其失败模式值得记住：

> Agent 会按指示调用一个不存在的方法，编译失败。**更麻烦的是 Agent 会怀疑自己理解有误，而不是怀疑经验有问题——陷入反复重试的恶性循环。**

设计上用于四处，**最小可用只接两个消费方**：

| 消费方 | `--mode` | 分档 |
| --- | --- | --- |
| 写入前自检 | `full` | ✅ 最小可用。命令是作者自己写的、在他自己机器上跑 |
| 注入前校验 | `symbols` | ✅ 最小可用。挂在每轮提示上，**不执行任何命令** |
| 定期审计 mem0 库 | `full` | ⬜ 延后，见 `docs/deferred.md` |
| 淘汰判定 | — | ⬜ 延后，见 `docs/deferred.md` |

**代码全部留在仓库**——推后的是两个调用点，不是那 898 行脚本与它的回归用例。第 2 类校验（符号存在性）从第一条经验起就适用：上面那个 `attachToQBListView` 的失败模式不需要库长大就会发生。

**没有它，`evidence_cmd` 只是一个没人跑的字符串。**

#### 重跑的执行边界

**`evidence_cmd` 是团队共享云端库里的一段文本，重跑它等于让任何能写入该库的人在每个成员机器上执行命令。** 红线过滤挡的是数据出去，这里挡的是代码进来——方向相反，同样必要。

**2026-09-02 的落地评审证明：只按程序名做白名单不构成边界。** 三条无 shell 元字符、通过程序名白名单、且全部返回 `pass` 的攻击被实测执行成功：

| 攻击 | 为什么过得去 |
| --- | --- |
| `/tmp/fakebin/git.exe` | 判白名单取 `basename`，执行却用原样路径 |
| `git ls-remote --upload-pack=/tmp/payload` | git 会真的拉起那个程序 |
| `find . -maxdepth 0 -fprintf /tmp/x line` | `\n` 由 find 展开，写出多行内容而不触发元字符过滤 |

同族还有 `rg --pre`、`git -c core.pager=`、`git config --global`（写宿主配置）、`npm install`（postinstall）、`go run …@latest`、`make -f`。**原先"测试入口跑的是本仓库自己的代码"这个理由是错的**——这些命令会从公网拉第三方代码执行。

第二轮评审证明**词法路径检查同样不构成边界**：

| 攻击 | 为什么过得去 |
| --- | --- |
| `cat ~/.gitconfig`、`cat ~/.ssh/*.pub` | MSYS 的 coreutils 从非 Cygwin 父进程启动时**自行**做 tilde 展开与 globbing，不经 shell 也一样；而 `~` 既非绝对路径也不含 `..` |
| `cat etc-link/hosts` | 符号链接在词法上完全合规。**任何平台都成立** |

两轮的教训是同一条：**边界必须画在它真正生效的那一层。** 在字符串上画，看不见 `~` 会变成什么，也看不见链接指向哪。

因此现在是**五道**：

1. **拒绝 shell 元字符、glob 字符、控制字符、`~` 开头、反斜杠转义的引号**——glob 是被调程序自己展开的，控制字符会让 `spawnSync` 抛异常
2. **不经 shell 执行**——直接 `spawn`，元字符即便漏网也不生效
3. **程序名必须是裸名**——含路径分隔符一律拒绝，由 PATH 解析，杜绝假冒二进制
4. **子命令与每一个选项都必须被显式允许**——未列举即拒绝，而不是列举危险项去拦
5. **每个路径形态的参数做 realpath 物理围栏**——解析符号链接后必须仍在 `realpath(--cwd)` 之内；位置参数的 `KEY=value` 右侧同样检查（`make SHELL=/tmp/evil.sh` 走的正是这条）

`find` `mvn` `gradle` `dotnet` 已整个移除——无法在选项层面收紧。`git branch` / `git tag` 已从允许的子命令中移除——它们是写操作，实测能在成员机器上真的建出 ref。`--allow` 追加的程序**不允许携带任何选项**。

**放宽只能靠改那张表，是一次有意的动作。**

**但"被拒"与"查不动"必须分开**——上一版把两者都算作失败，于是一台没装 `rg` 也没装 `grep` 的机器跑一次审计会把全库标为失效。现在每条结果带 `verified`：**淘汰只对 `verdict: fail` 且 `verified: true` 生效；注入前校验把 `verified: false` 一律当作不可采信。**

**一项已知残留**：超时只杀直接子进程，孙进程会活下来（`npm test` / `go test` 都是起一堆 worker 的形态）。收紧允许列表之后，能起进程树的只剩本仓库自己的测试命令，因此这是资源问题不是安全问题——但它确实存在。

#### `evidence_digest` 的可判定形式

「退出码 + 输出摘要」需要一个能机器比对的形状。**不比对完整输出**——重跑几乎不会逐字节相同（时间戳、路径、耗时）：

```json
{ "exit": 0, "contains": ["ok  "], "absent": ["FAIL"] }
```

这个形状既稳定，又抓得住"跑了但抄错"——前身实测过的形态是：原始输出 11 行就贴在紧邻处，结论里却写了 10。

**三项均可省，但省略 `exit` 等于声称 `0`。** 「命令跑不通」必须能被检出，否则淘汰判据拿不到信号。**因此复现型命令（期望非零退出码）必须显式写 `exit`**，否则会被判失败——这一条由 Review 层强制。

前身的另一条教训适用于本项目的所有形状检查：**形状约束不阻止绕过，只是让绕过留下可扫描的痕迹——你还得有那个扫描器。** 审计就是扫描器。

## 架构记录与模块级知识——大部分仍延后

模块级知识的分流规则、`architecture-notes` skill（怎么**写**架构文档），**仍在 [`docs/deferred.md`](docs/deferred.md)**。

理由：它们管的是写文档的人，而目标四的失效发生在读的那一刻。

**已于 2026-09-04 拿回的是「读时的优先级约束」**——原设计把它交给 `.claude/rules/` 路径作用域规则，而**插件不能提供 rules**，那条路装不上；换成 `PostToolUse` 的 hook 之后失效点没变，见上「这三条的执行者」。规范类 / 描述类 / ADR 的三分本来就在上面那张表里，未移出过。

## 经验如何沉淀

**云端 mem0，写入与自动检索都经脚本走 REST；MCP 那一组读工具留给 agent 临时查用。**

**这一句原先写的是"检索经 MCP"，落地时发现不成立，如实更正**：自动检索挂在 `UserPromptSubmit` 上，而该事件只支持 `type: "command"` 的 hook（`agent` / `prompt` 两型只对工具事件开放）——一个命令脚本调不到 MCP 工具。因此每轮的检索走 REST，与写入共用 `scripts/mem0.js` 一份契约：**契约若是错的，两条路一起错，改一处即可；各写一份则会出现"写得进去但检索不到"，而两端都不报错。**

`.mcp.json` 里的 mem0 server 仍然保留，作用是让 agent 能主动查库（读工具不在 deny 之列）。端点 `https://mcp.mem0.ai/mcp`，OAuth 或 API key bearer。

### 写入触发——已延后

四条可观测信号（hook deny、测试由绿转红、工作区外动作、本轮提示构成一次纠正）经 hook 唤起 `experience-intake`，**整体移入 [`docs/deferred.md`](docs/deferred.md)**。

理由：**"能写进去"与"一定会写进去"是两件事，最小可用只要求前者。** 用户说一句"把这个记下来"、agent 按 description 加载 skill、走写入脚本入库——这条路径在没有触发 hook 时完整成立。触发 hook 解决的是覆盖率，是对一条已跑通路径的加固。

**代价如实记下**：该 hook 的失效后果原文写的是「库长期为空且无人知晓」。`docs/deferred.md` 里给这一项的拿回条件写成了可观测的量，不是"看着办"；其中第四条（`UserPromptSubmit` 上的纠正判定）成本接近于零，应第一个拿回。

### 写入路径：脚本编排三次独立判定

**产出经验的 agent 不能同时裁决它。**

腾讯团队对同一形状的结论是：「**让 AI 自己判断自己产出经验的价值，等于让裁判同时当运动员——在工程上无效**」，其初版正是"抽取即入库"，**约 90% 是废经验**。这与前身 Themis 的 `实现者不得验证自身产出`（11 次失效 10 次）是同一结构问题的两次独立观察。

解法**不是加人工审批**，而是把治理拆成**各自独立上下文的三次判定**，由 `scripts/experience-write` 编排：

| 层 | 目标 | **默认方向** | 依据 | 分档 |
| --- | --- | --- | --- | --- |
| **Review** | 拦明确垃圾 + 事实性校验 | **默认保留，定向过滤** | 九类垃圾特征 + `assert-replay` | ✅ 最小可用 |
| **Dedup** | 候选集内去重 | **宁严勿宽** | 否决式规则 | ⬜ 整层延后 |
| **Merge** | 与历史库的关系 | **保护历史边界** | create / update / skip / contradict | 只留 `create` + 甲分支晋级 |

**三层的默认方向必须不同**：漏掉一条好经验的代价大于放行两条边缘经验（故 Review 默认保留）；**误合并造成的边界污染不可逆**（故 Dedup 宁严勿宽）；已入库的是已验证资产，新经验要跨更高门槛才能改它（故 Merge 保护历史）。**"宁可错杀"和"宁可放过"的方向在各层完全相反，用一个 prompt 一次判完，各层目标会互相牵制。**

三层的判据分别存放在 `skills/experience-intake/{review,dedup,merge}.md`，脚本各起一次判定、只喂对应那一份——**`SKILL.md` 本身保持短**。

**分档说明**：三层里只有 **Review 判的是单条经验本身**，从第一条起就适用——腾讯团队那 90% 的废经验率是"抽取即入库"从第一天就有的，不是库长大之后才出现的。Dedup 判的是**候选集内**的关系（需 ≥2 条候选），Merge 的 `update`/`skip`/`contradict` 判的是**与历史存量**的关系（需库里有条目）；空库时两者的输入都不存在。因此 Review 层完整保留，另两层的延后部分见 [`docs/deferred.md`](docs/deferred.md)。

**这不表示"治理已经做过了"**：90% → 5% 是三层合起来打出来的，无法拆分归因到某一层。最小可用阶段的废经验率不等于 5%，具体多少无从预测——这是分档的代价。

完整流程：**抽取 → Review → Dedup → Merge → 红线过滤 → 带 `infer=False` 写入 REST API**。

按上表分档，**最小可用跑的是：抽取 → Review → Merge(`create` + 甲分支晋级) → 红线过滤 → 带 `infer=False` 写入 REST API**。Dedup 一步的位置在流程里保留不动，拿回时原样插回。

同时 hook **deny 裸调用 mem0 的写入与删除工具**。写入必须走脚本，四个理由：

1. **查库变成不可跳过的机器动作。** Merge 层要求"写入前先查历史库"——若这一步留在 skill 里，跳过它的后果很具体：同一错误堆成 N 条互不相识的候选，**永远到不了第二次，重复检测静默死亡且无处记录**。
2. 红线过滤有地方放（见下）。
3. **前提假设 5 的退路落在这里**：写入失败必须硬失败、不得静默丢弃——那是脚本的行为，不是 hook 的。（原先并列的"假设 1 的退路"已不适用：`infer=False` 逐字保留于 2026-09-03 实测转为事实，不再是假设。）
4. 省掉一条独立的形状检查 hook——脚本自己就是校验点。

### 抽取判据

**判定一条信息是不是经验，标准只有一个：被召回后，Agent 能否产生正向的行为变更。**

由它推出两道判据，**顺序不可颠倒**。

#### 第一道：可发现性——三镜头（必要条件）

> **如果 Agent 自己就能推出来，召回它没有任何额外价值。**

按**认知障碍**分类，不按主题或重要性——那些维度要么列不完，要么依赖主观判断；**"Agent 能不能自己发现"是客观可判定的**。

| 镜头 | 障碍本质 | 例 |
| --- | --- | --- |
| **黑话** | 语义不可发现 | 项目内部的缩写、代称，字面完全无法推导 |
| **索引** | 位置不可发现 | 某能力入口不在直觉路径上，读代码找不到，需要知道"去哪找" |
| **逻辑** | 行为不可发现 | 反直觉的工程约束、隐式机制，常规推理推不出来 |

**三镜头都不命中即不记录**，无论它多高频、代价多大。

#### 第二道：值不值得占位

三镜头命中之后，再判它是否够格进入检索：

**甲——重复出现**（在 Merge 层判定）。官方实践的原话是"Claude 犯错两次，更正就写进 `CLAUDE.md`"，但**那条规则的执行者是人**。本设计的写入路径里没有人，agent 跨会话、跨成员都无从知道这是第几次。因此把"是不是第二次"**从记忆问题改成查询问题**，由脚本强制执行：查不到 → `status: candidate`，不进入检索；查得到 → `occurrences + 1`，翻为 `confirmed`。

##### 什么叫"查得到"——同型判定的最小判据

**"查得到"必须有定义，否则甲分支两头都会死**：判得太严，全库停在 `candidate`、一条都不进检索；判得太松，不相干的候选互相晋级、甲分支等于没有闸门。而它**不能像 `update`/`skip` 那样"判不准就 `create`"兜底**——那个兜底在这里就是"永不晋级"本身。

**两个方向的代价不对等，这决定了默认方向：**

| 误判方向 | 后果 |
| --- | --- |
| 漏判同型 | 库永远是空的——**最小可用那句话的后半直接不成立** |
| 误判同型 | 一条**已过三镜头与九类垃圾检查**的经验提前进检索，且仍要过注入前的 `assert-replay`，且可被 `retracted` |

因此**甲分支宁宽勿严**，与 Dedup 的「宁严勿宽」方向相反。不矛盾——两层的动作不同：**Dedup 合并会永久丢掉边界信息，甲分支只改一个 `status`，不动内容。**

**判据：两把性质不同的锁同时合上，才算同型。**

```text
语义锁   在 search_memories 召回的前 5 条之内   ← 相似度由 mem0 判，本地不重算
结构锁   symbols 交集非空
         或（repo 相同 ∧ files 交集非空）
```

- **`files` 必须配 `repo`**：`README.md`、`src/index.ts` 在任何仓库都有，单独拿它当锚，它就是那座桥。
- **`symbols` 可单独成锚**：项目内标识符跨仓库重名概率低——**这也让跨仓库的同一框架坑仍然判得出来**。
- **不做传递闭包**：只判新条目与每条召回结果的**直接**关系；多条命中取召回排名最前的一条为 `promoted_from`，其余不动。桥接式合并（A~B、B~C ⟹ A~C）因此在结构上不可能发生，Dedup 那套否决规则不必提前拿回。
- **用 `top_k = 5` 而不是相似度阈值**：没有实测过的阈值，凭空写一个 `0.85` 正是「数字必须来自实跑」要防的那件事。`5` 与注入条数上限同源，不是新造的数。**可证伪判据：出现过一次"该判同型却排在第 6 位"，就是 5 太小的证据。**
- **结构锁的交集计算放写入脚本，不交给模型**——让模型判"是不是同一个坑"，就是让产出经验的人同时裁决它。

**晋级时不改动老候选**：新条目写 `status: confirmed`、`occurrences` 取老值 + 1、`promoted_from` 指向老候选。代价如实记下——**每次晋级在库里留下一条永不被检索的死候选**（`candidate` 不进检索），那属于库膨胀，处置在已延后的淘汰一节。

**并发竞态须由脚本处理**：A、B 同时首次撞上同一错误，各自查不到、各写一条候选，此后两条互为"第一次"、**永不晋级**。做法是**写完候选后延迟复查一次**：等 ≥ 725 ms（实测的异步索引延迟）再查一遍，若这次查到另一条同型候选且不是自己，说明并发发生，走晋级路径。这个延迟在**写入路径**上，不在每轮提示上。

**乙——单次高代价。每条信号必须绑一个写入方无法自由撰写的锚，取不到锚即不许走乙分支，退回存候选。**

| 信号 | 必附的锚 |
| --- | --- |
| 人纠正过 | 用户原话**逐字引用** + 轮次定位 |
| 同一 `evidence_cmd` 连续失败 ≥ 3 次 | 三次的命令与退出码 |
| 产生了工作区之外的影响 | 具体动作与目标 |
| 被闸门拒绝过 | 被拒的 hook 名与时间（排除本项目自身闸门） |
| 弄坏了已通过的测试或 CI | run id 或命令退出码 |

**乙分支直接写 `status: confirmed`、`occurrences: "1"`。** 它的全部意义就是绕过"必须出现两次"，写 `candidate` 等于乙分支不存在。**它的闸门不是次数，是锚**：`high_cost` 与 `high_cost_anchor` 必须齐备且可核，取不到锚就降级写 `candidate`、回到甲分支。代价如实记下——乙分支一次入检索，它的全部过滤就只有三镜头、九类垃圾与 `assert-replay` 三道。

**锚是必需的，因为形状检查只能验证"字段存在且取值在枚举内"，验证不了它为真。** 前身实测过精确的反例：详细设计把 Agent 自己的推论包装成人类评审者的要求，**引用格式完全合规，只是批复原话里根本没有那句**，静态检查与人工通读都放得过去。其结论是：**凡声称某项决定来自人类，都必须可追溯到逐字原话；追溯不到即拦。**

#### 每条经验必须自证

产出时同时给出三项，否则 Review 层拒收：

- **为什么这是经验**——命中哪个镜头、解决了什么可复用问题
- **通过了哪些垃圾检查**——见下九类
- **证据是什么**——哪几轮对话、哪条命令的输出支撑它

### Review 层：九类垃圾特征

**先研究垃圾，再反推好经验**——"好经验"难一次定义到位，"垃圾"的特征更容易归纳。这九类是腾讯团队把垃圾率从 90% 打到 5% 的起点，直接采用：

| 特征 | 说明 |
| --- | --- |
| 事实性错误 | 编造不存在的约束，把对话里的临时说法当规则 |
| 通用常识 | 任何项目都适用，不体现团队特异性 |
| 对话摘要 | 只复述过程，没有沉淀成可复用判断 |
| 一次性 case | 只对当前任务有效，不能迁移 |
| 用户主观偏好 | 个人选择，不代表团队规范 |
| 缺少上下文 | 不知道对应哪个模块/组件/接口 |
| 粒度混用 | 一条里兼顾通用规则和 case 特定信息 |
| 不可执行 | 只有抽象建议，Agent 看完不知道怎么做 |
| 证据不足 | 对话里没有足够依据，模型自己推断过多 |

「事实性错误」一类**不靠文本判断**，交 `assert-replay` 做符号存在性与命令重跑校验。

### Dedup 层——已整层延后

否决式的五条不得合并规则（含★禁止桥接式合并）、"判不准就新建"、以及那组参照数据（F1 71.79%、严格重复 Recall 91.67%、batch 间波动到 6 个正例只识别出 1 个），**整体移入 [`docs/deferred.md`](docs/deferred.md)**。

理由：它判的是**一次抽取产出的多条候选之间**的关系，而最小可用的形态（一次踩坑 → 一条经验）不产生这个输入。且它的默认方向就是"判不准就新建"——**这一层失效时的行为，恰好等于它不存在时的行为**。

### Merge 层：只留 `create` 与甲分支晋级

最小可用里 Merge 层只做两件事，两件都决定"什么能进入检索"，缺一则经验取不回来：

| 动作 | 何时 |
| --- | --- |
| `create` | 库中不存在同类目标 |

加上甲分支的晋级：查库 → 查不到写 `status: candidate`；查得到则 `occurrences + 1` 并翻为 `confirmed`。**"查得到"的判据是语义锁 ∧ 结构锁**，连同不做传递闭包、晋级不动老候选、并发竞态的延迟复查，全部见上「什么叫"查得到"」。

**`update` / `skip` / `contradict` 三个动作、唯一目标判定前置、`update` 自检的四问，全部移入 [`docs/deferred.md`](docs/deferred.md)**（含参照数据 update Recall 96.30% / Precision 78.79% 的"积极合并"倾向）。

理由：三者都要求"库里已有相关条目"作为输入，空库时触发条件不存在。**且推后它们不改变任何行为**——原设计写明「没有唯一目标就默认回退 `create`」，推后等于让这条设计自带的默认路径成为唯一路径。

`status` 枚举里的 `contradicted` 与 metadata 里的 `contradicts` 字段**保留在 schema 中不动**，推后的只是写它们的路径。

### 条目结构

经验本身不是事实，但必须携带指向事实的指针：

```text
information:  一句话——什么做法导致什么错误
metadata:
  repo, commit      发生时的代码状态
  evidence_cmd      能复现或证伪它的命令（由 assert-replay 实际重跑）
  evidence_exit     声称的退出码，字符串（省略等于声称 "0"）
  evidence_contains 输出必须包含的特征串        ┐ 摊平存放：mem0 的 metadata
  evidence_absent   输出必须不含的特征串        ┘ 是字符串键值模型，嵌套对象
                                                会被扁平化，不能原样往返
  files             仓库相对路径（绝对路径与 .. 一律拒绝）
  symbols           本条声称存在的标识符，如 ZzWidget.attachToList
                    —— 由 Review 层抽取；脚本无从猜测该查什么
  author, at        谁写的（mem0 侧无归属，必须自带，故不可信，仅供线索）
  lens              blackword | index | logic —— 命中哪个镜头，缺失即拒收
  status            candidate | confirmed | contradicted | retracted
                    —— 无 stale：它曾是 assert-replay 自动写回的结果，
                       而判定结果不回写（见下）。撤回由人写 retracted
  occurrences       出现次数
  promoted_from     走甲分支时：被撞上的那条候选的 memory_id
  contradicts       走 contradict 时：与之冲突的 memory_id
  high_cost         走乙分支时：五条信号中的哪一条
  high_cost_anchor  该信号对应的锚（逐字原话 / 退出码 / run id 等）
  recalled_n        被召回次数        ┐ 淘汰判据，
  adopted_n         被采纳次数        ┘ 由召回旁路回填
  expiration_date   YYYY-MM-DD，到期后默认不再被检索到
                    —— 消费方已延后，见 docs/deferred.md 的淘汰一节
```

**所有值只用字符串与多元素数组。** 数字会被转成字符串、单元素数组会降级成标量——读取端一律过 `toArray`，比较退出码前转数字。这不是洁癖：`"0" !== 0` 会让每一条往返过的经验判失败，而它们全部会走淘汰。

`files` 与 `symbols` **各有数量上限**（初始各 20，超出即判失败）。上限防的是单条条目失控——实测一条 300 符号的条目耗时 13.5 秒；**整轮延迟不靠这个上限控，靠批量检索**，见下「注入前校验的延迟预算」。

**只有 `status: confirmed` 进入检索。** `contradicted` 暂停召回但保留，`retracted` 永久排除。

**没有 `last_checked`，也没有任何校验缓存。** 前者原本是 `assert-replay` 的判定回写，而判定结果不回写；后者已于 2026-09-04 明确决定不做。每轮注入前全部重跑。

### 红线与撤回

**红线过滤（写入脚本内强制）**：`evidence_output` **不落原文**，只留退出码与摘要；`files` 只留仓库相对路径。

理由：命令原始输出可能含密钥、内网地址、客户数据或私有代码，一旦推到第三方云服务**就拿不回来**。

**撤回路径**：经同一个写入脚本写 `status: retracted`，检索排除。deny 只针对 agent 的裸删除工具。**撤回不是人工审批，不违反"skill 是唯一裁决者"。**

没有撤回路径的话，一条错误的 `confirmed` 会被每轮注入，而它不能删（hook deny）、不能评审（无 diff）、不能归责（`author` 自填）。

### 淘汰、库审计与 `expiration_date`——已整节延后

人工触发的离线库审计、两条报告判据（`verdict: fail` 且 `verified: true`；`recalled_n ≥ 10` 且 `adopted_n = 0`）、"为什么不能自动"的论证、以及 `expiration_date` 的自然退场机制，**整体移入 [`docs/deferred.md`](docs/deferred.md)**。

理由：**淘汰的对象是"库随时间静默膨胀"与"历史经验集体失效"，一个还没写进过一条经验的库两者都不存在。** 原文引的「生产决定基线 → 使用暴露问题 → 维护反哺生产」自己就写明了顺序，维护是第三环。且第二条判据依赖 `adopted_n`，而那个观测手段**当前无解**（见下「用途限定与它的收益上限」）。

`recalled_n` / `adopted_n` / `expiration_date` 三个字段**保留在 schema 中不动**，推后的是它们的消费方。

### 检索：`UserPromptSubmit`，带双上限

**不挂 `SessionStart`**——那一刻用户还没提问，`search_memories` 没有查询词，只能注入全部（= 571 行控制面的云端版）或注入无关内容；且它一次性触发，会话中途换话题就没有第二次检索。

挂 `UserPromptSubmit`：query 就是用户这一轮的提示，天然有上下文，每轮都触发。

**注入须设双上限**（条数 + token），超出即截断，并**计入统一的上下文预算**。

**校验在注入前完成，不是让 agent 自己去跑。**

"取回后请先跑 `evidence_cmd` 再采信"是一条对 agent 的要求，没有执行者——正是 TRACE 那 57.5% 的形状。改为**注入前的机器动作**，由检索脚本执行：

1. 检索命中若干条 → 逐条跑 `scripts/assert-replay`
2. **`verdict` 为 `fail`、或 `verified` 为 `false` 的条目一律不注入**
3. 通过的条目注入

超时兜底：单条校验超时按跑不通处理（不注入），避免校验拖慢每一轮提示。

#### 注入前校验的延迟预算——批量检索，不做缓存

**决定（2026-09-04）：不引入任何校验缓存。** 预算因此只能靠减少工作量来平，不能靠记住上一次的结果。

原先的账平不了：符号校验是**每个符号一次全仓检索**，5 条 × 20 符号 = 100 次扫描。实测（Themis 仓库，405 个跟踪文件、18 MB，ripgrep 15.1.0）：

| 100 个符号 | 逐个调用 | 一次批量 |
| --- | --- | --- |
| `rg` 后端 | 6,404 ms | **122 ms** |
| `grep` 后端 | 16,843 ms | **205 ms** |

**成本几乎全在"扫一遍仓库"这个固定开销上，不在符号数量上。** 一次调用带 100 个 pattern 与带 1 个 pattern 扫的是同一遍。改批量后，最差的机器（只装了 `grep`）也落在 2,000 ms 预算的十分之一。

**逐符号的判定不会因批量而丢失**：`-F -w -o` 输出的匹配串就等于 pattern 本身，去重后即"命中了哪些符号"，未出现的即不存在。实测 3 个存在 + 2 个不存在的批量检索，返回的正是那 3 个。

`--budget` 的语义随之改为**整轮一次**（原为逐条），与「每轮注入 ≤ 2,000 ms」同一个单位。

**已落地**（`assert-replay.js` 的 `prescanSymbols`，2026-09-04）。批量是性能改动，**判定必须逐符号等价**——否则同一条经验在两台机器上命运不同，而两边都不报错。守这条的用例：两个后端各跑一遍逐个查与批量查，逐符号比对结论（含只存在于被 `.gitignore` 忽略目录里的那个符号）。对照实测：fixture 上 40 个符号，逐个 1,767 ms、批量 45 ms。

**批量拿不到结论时不降级成"符号不存在"**，判为查不动（`verified: false`）——两者混流的后果是一台没装 `rg` 也没装 `grep` 的机器跑一次审计就把全库判失败。

### 判定结果不回写共享库

**这是硬约束，不是优化。**

`assert-replay` 给出的 pass / fail，一旦以 `status` 或 `last_checked` 的形式写回 mem0，**写这条经验的人就读得回来**。而 `evidence_cmd` 由他自己写：

```text
写入一条 evidence_cmd: grep -r -q -F <猜测> .
  → 每个成员的每轮提示都会跑它
  → 命中 pass / 不中 fail
  → 若回写，攻击者读回这一位
```

条目数量不受限，可并行爆破。**红线过滤挡不住**——它防的是 `evidence_output` 原文外流，而这里泄漏的载体是**判定本身**，不在那条路径上。

因此**逐条判定结果只留在本地**。后果如实记下：

- **没有 `last_checked` 可缓存**，每轮注入前都要重跑；**本地缓存也明确不做**（2026-09-04 决定），靠条数上限（≤5 条）与批量符号检索把账平掉，见上「注入前校验的延迟预算」
- **淘汰失去自动信号**，改为人工触发的离线审计（该方案已延后，见 `docs/deferred.md`）

不受此约束的是 `recalled_n` / `adopted_n`：它们由用户提示驱动，不是 `evidence_cmd` 的函数，攻击者无法用它们问出任意问题。

仍随附的使用约束只剩一句——**与当前代码冲突时以代码为准**——它不要求 agent 主动做什么，只在冲突发生时给出优先级。

### 用途限定与它的收益上限

**只作检索参考**，不编译成必过检查。

**但要承认这条路的天花板**：TRACE 数据（57.5% 取到了也照样违反）量的正是"检索参考"这条路。**目标三的预期收益上限被这个数标在四成左右**——但这只是量级参考，**该数的口径未本地记录**（见第一原则一节的注），不能当作精确阈值使用。

腾讯团队跑通之后给出同一个观察，措辞更直接：**"命中不等于采纳——目前缺乏观测 Agent 是否真的依据经验改变行为的手段。"** 他们的召回率已达 68.8%，但采纳率无从得知。

因此必须度量：**经验被检索出来之后，有多少比例真的改变了行为**——这是目标三唯一有意义的指标，也是淘汰判据里 `adopted_n` 的来源。**这个观测手段目前无人解决，本项目也没有方案**，先记为已知缺口。（淘汰本身已延后，见 `docs/deferred.md`。）

**「经验 → 永久 eval」的升级路径已移入 [`docs/deferred.md`](docs/deferred.md)**——它原本就标着"当前不做"。注意它与上面那条取舍的边界：**「不把经验编译成必须通过的检查」是 `Intent.md` 五.1 明确不做的，不因分档而改变**；真要走那条路是一次意图变更，须明说。

### 选云端 mem0 放弃了什么

跨仓库、跨工具、跨机器共享是本目标的硬要求，仓库内 `CLAUDE.md` 做不到。代价是真实的：

| | 仓库内 `CLAUDE.md` | 云端 mem0 |
| --- | --- | --- |
| 共享范围 | 单仓库 | 跨仓库/工具/机器 |
| 可评审 | ✅ 走 PR、可 diff、可归责 | ❌ 无 diff，`author` 自填 |
| 逐字保留 | ✅ 就是文件 | ⚠️ 依赖 `infer=False`（前提假设 1） |
| 数据边界 | 不出仓库 | 出到第三方，故需红线过滤 |
| 上下文成本 | 每会话全量读 | 按需检索 |

---

# SKILL 清单

| skill | 管什么 | 来源 |
| --- | --- | --- |
| `experience-intake` | 经验准入的唯一裁决者。`SKILL.md` 保持短，判据分置 `review.md` / `dedup.md` / `merge.md`，由写入脚本各起一次独立判定、只喂对应那份 | 新写 |

**最小可用里只有这一个。** 原设计的四个 skill 中，`review-presentation`、`architecture-notes`、`intent-lock` **整体移入 [`docs/deferred.md`](docs/deferred.md)**（`adr-write` 与"架构 skill 不拆成两个"的两条明确不设，随架构一节一并移入）。

**最小可用交付的判据文件是 `review.md` 与 `merge.md` 的 `create` 部分；`dedup.md` 与 `merge.md` 的另三个动作延后**，理由见上「写入路径」一节的分档说明。

仍然明确不设的一条：

- **不设 `experience-recall`**：检索触发交给 hook，剩下的"取回来怎么用"是三句话，随注入内容一起给出。

# hook 清单

| 事件 | matcher | 作用 | 失效后果 |
| --- | --- | --- | --- |
| `UserPromptSubmit` | — | 按本轮提示检索 mem0，注入前先校验、带双上限 | 经验取不到 |
| `PreToolUse` | `mcp__plugin_experience_mem0__(add_memory\|update_memory\|delete).*` | 一律 deny，写入只能走脚本 | 查库可被跳过，甲分支静默死亡；库可被清空 |
| `PostToolUse` | `Read` | 读到描述类架构 / spec / ADR 时注入该类文档的优先级约束 | 目标四在"读的那一刻"没有执行者 |

**三条。** 另两条移入 [`docs/deferred.md`](docs/deferred.md)：写入触发（`PostToolUse` / `Stop` 三条信号 + `UserPromptSubmit` 上的纠正判定）、意图偏离检查（含它的两种形态与前提假设 4）。

**三条的失败方向不同，这是有意的，改动前先看清楚：**

| hook | 解释器缺失时 | 为什么 |
| --- | --- | --- |
| `deny` | **失败关闭**（`\|\| exit 2`） | `PreToolUse` 只有 exit 2 才阻断；放行等于绕过写入脚本的全部把关 |
| `recall` | **失败开放** | `UserPromptSubmit` 上非 0 会**挡住用户这一轮提问**——"取不到经验"绝不能升级成"不许提问" |
| `fact-priority` | **失败开放** | `PostToolUse` 上非 0 会把错误抛回给模型；**读一个文件不能因为一条附加 hook 而出问题** |

**把后两条照抄成 `|| exit 2` 就是把它们改坏**，有用例守着。

**"deny 已生效而替代路径不存在"这项当前状态已于 2026-09-04 解除**——`scripts/experience-write.js` 已落地。在它落地之前，装上这个插件对用户是净负能力的（拦掉了 mem0 裸写入，替代路径不存在），那正是把它排为最小可用唯一解锁点的理由。

**matcher 有两个会静默失效的陷阱，都不报错：**

1. **前缀形态**：插件提供的 MCP server，工具名是 `mcp__plugin_<plugin-name>_<server-name>__<tool>`，**不是** `mcp__<server-name>__<tool>`。本项目 plugin 名 `experience`、server 名 `mem0`，因此前缀为 `mcp__plugin_experience_mem0__`。**写成 `mcp__mem0__` 匹配不到任何工具。**
2. **精确匹配**：只含字母数字与 `_ - , |` 的匹配值按**精确字符串**比较。所以末尾的 `.*` 或括号是必需的——没有正则字符，整条就退化成精确串。

两者叠加意味着一个看起来合理的 matcher 可以完全不生效而毫无提示。**改动 plugin 名或 server 名时必须同步改 matcher**，并由 eval 用例守住。

**hook 脚本不得依赖 `jq`**（本机未安装，官方示例几乎全用它，会静默失败），路径一律用 `${CLAUDE_PLUGIN_ROOT}`。

# 脚本清单

| 脚本 | 作用 |
| --- | --- |
| `scripts/experience-write.js` | ✅ 已落地。形状 → 红线 → 写入自检 → 查库 → 甲/乙分支 → `infer=false` 写入 → 候选的并发延迟复查 |
| `scripts/assert-replay.js` | ✅ 已落地。重跑 `evidence_cmd` 并比对 + 校验符号存在性。**两个消费方均已接上**：写入自检（`--mode full`，由 `experience-write` 调）与注入前校验（`--mode symbols`，由 `hooks/recall.js` 调）；库审计与淘汰判定两个消费方已延后 |
| `scripts/mem0.js` | ✅ 已落地。REST 客户端，**写入与检索共用一份契约**——各写一份则会出现"写得进去但检索不到"，两端都不报错 |
| `scripts/auth-headers.js` | ✅ 已落地。`.mcp.json` 的 `headersHelper`——把 key 注入 MCP 连接而**不让它进任何文件**；取不到 key 时响亮地失败 |
| `scripts/selfcheck.js` | ✅ 已落地。**装到使用者项目后，让他验证这些在他那儿真的生效** |

hook 侧另有三个脚本：`hooks/deny.js`、`hooks/recall.js`、`hooks/fact-priority.js`，见上「hook 清单」。

**REST 契约本身尚未对活端点验证**：路径、鉴权头形状、请求体字段名来自公开文档，本机没有可用的 key。已实测的只有 MCP 那一侧。

验证手段是消费方拿**自己的** key 在**自己的**项目里跑 `selfcheck.js --mem0`——**凭据不必进这个仓库，也不必给任何人**。它覆盖的范围要说清楚：

| | 覆盖 | 怎么覆盖 |
| --- | --- | --- |
| `searchPath` + 鉴权头形状 | ✅ | 一次**只读**检索；`Token` 与 `Bearer` 都试，报告哪种成立，与 `mem0.js` 写的不一致就判失败 |
| 响应容器形状 | ✅ | 认 `顶层数组` / `results` / `memories` 三种，都认不出就判失败 |
| `addPath` | ❌ | **自检不往使用者的共享库里写探针**——那是用探活污染真实数据。写入路径只能由第一条真经验来验 |

**这段话有过一次教训**：2026-09-04 之前 `selfcheck --mem0` 只探 MCP、一次也没碰过 REST，而三个文件都写着"验证手段是它"。**声称有执行者，而执行者不在那一层**——本项目要防的形状，这次发生在自己身上。

**为什么要自检**：本插件的强制点全部是静默失效型的——deny 闸门在 node 不在 PATH 时放行且不留痕，符号检索在只有 `grep` 的机器上慢数倍，MCP 连不上时检索静默为空。**这些失效在正常使用中看不见**，使用者会以为闸门在、经验在，实际都不在。

**而它们在使用者环境里比在开发机上更可能发生**：环境异质、仓库更大（符号检索成本随规模增长）、node 未必在 PATH。`selfcheck.js` 逐项实跑并打印结果——包括把解释器换成不存在的程序、确认闸门仍以 exit 2 阻断——**输出设计成可直接贴回来的形式**，不是绿勾。

**安全状态**：两轮独立评审各实测出一批可执行的攻击，全部已修，每条都有回归用例（`evals/assert-replay.test.js`，共 297 项）。边界模型经两次下沉——先从"程序名白名单"到"子命令 + 逐选项显式允许"，再从"词法路径检查"到"realpath 物理围栏"。**第二轮修复尚未经独立评审。**

脚本用 Node 实现并以 `node <path>` 调用（不依赖 shebang，避免 Windows 上的可执行位与解释器解析问题）。**这引入一项对 Node 的依赖**——不用 `jq` 已是硬约束，而在无 JSON 工具的纯 shell 里处理条目不现实。

**符号存在性检查的边界**：它是语言无关的标识符存在性检查（该标识符在仓库中是否出现过），**不判断归属**。它抓得住"编造一个不存在的符号"，抓不住"符号存在但归属错了"。判断归属需要按语言接语言服务，尚未做。

---

# 仓库结构

```text
project-agent-harness/
├── .claude-plugin/marketplace.json     # source 相对本目录的父目录解析
├── plugins/experience/
│   ├── .claude-plugin/plugin.json      # 只有这个文件放这里
│   ├── .mcp.json                       # 云端 mem0（仅检索）
│   ├── hooks/
│   │   ├── hooks.json                  # 三条，失败方向见「hook 清单」
│   │   ├── recall.js                   # UserPromptSubmit：检索 → 校验 → 注入
│   │   ├── fact-priority.js            # PostToolUse/Read：读到架构/spec/ADR 时降级
│   │   └── deny.js                     # PreToolUse：拒绝裸调用写入工具
│   ├── scripts/                        # experience-write / assert-replay / mem0
│   │                                   # / auth-headers / selfcheck
│   ├── skills/
│   │   └── experience-intake/
│   │       ├── SKILL.md                # 短；抽取判据 + 指向下面三份
│   │       ├── review.md               # 三镜头 + 九类垃圾，默认保留
│   │       ├── dedup.md                # 否决式规则，宁严勿宽（延后，未建）
│   │       └── merge.md                # 甲/乙分支；四动作留待拿回
│   └── README.md                       # 模块核心设计随包走
├── evals/                              # 7 份 487 项；run-all.js 自动发现 *.test.js
├── .github/workflows/evals.yml         # 门禁：双 OS matrix，首次运行 2026-09-04
├── DESIGN.md
└── docs/
    ├── deferred.md                     # 分档移出的设计：原文 + 理由 + 拿回条件
    ├── decisions/                      # ADR
    ├── references/                     # 外部参考的提炼
    └── review/                         # 评审发现与逐条处置追踪
```

**树中 `dedup.md` 属延后项**（见 `docs/deferred.md`），`merge.md` 在最小可用里只承载 `create` 与甲分支晋级。目录结构本身不变，拿回时按原样填入。

`commands/`、`agents/`、`skills/`、`hooks/` 必须在 plugin 根目录，**不能放进 `.claude-plugin/`**。开发期用 `claude --plugin-dir ./plugins/experience`，改完 `/reload-plugins`，提交前 `claude plugin validate`。

---

# 约束

## 上下文预算

前身的失败是 571 行常驻控制面。本项目：

- **常驻成本 ≤ 400 tokens**：全部 skill 的 `description` 之和 + hook 配置
- **每轮注入 ≤ 1,500 tokens 且 ≤ 5 条**：经验条目 + 使用约束，超出即截断
- **每轮检索 ≤ 1,500 ms**：网络往返，超时即注入空。**这一条是落地时新增的**——原设计只框住了校验，而检索同样挂在每轮提示上，只框住其中一半等于没框
- **每轮注入前校验 ≤ 2,000 ms**：由 `assert-replay --budget` 强制，**整轮一次而非逐条**；超预算的条目判为**未验证**、不注入。实测账见「注入前校验的延迟预算」
- 两条合计**最坏约 3,500 ms/轮**，由 `hooks.json` 的 `timeout: 10` 兜底

**三条分列，因为增长方式不同**：第一条固定，第二条随库增长，第三条随库与仓库规模一起增长。只框住第一条等于框住了不会长的那部分。

**第三条是延迟预算，与前两条并列而非附注。** 前身死于每会话读 571 行；若给每轮提示挂一个无上界的同步阻塞，是同一个病换了个单位。这个阈值原先只是一个 CLI 默认值（30 秒 × 5 条 = 最坏 150 秒/轮），**任何人可以任何理由改动而不触发下面"改阈值必须改这里的数字"那条规矩**——那正是「规则写下了但执行者不在那一层」的形状。

- 单个 SKILL.md ≤ 250 行
- `claude plugin details <name>` 核组件清单与预估 token 成本
- `InstructionsLoaded` hook 核**实际**加载了哪些指令文件、何时、为何

以上为初始值，须以实测修正。**"实测修正"不是超标的豁免**：改动阈值必须同时改动本节数字并说明依据。

## 维护面预算

**常驻上下文轻，不等于维护面轻。** 原设计的账：4 skill（其中 `experience-intake` 带三份判据文件）+ 4 hook + 2 脚本 + 20 字段 schema + `.claude/rules/` 分流规则 + 5 条前提假设。**"会不会再变庞大"该量的是这个总和**，不只是 400 tokens。

**分档时估的账**：**1 skill**（`experience-intake`，带 `review.md` + `merge.md` 两份判据）+ **2 hook** + **4 脚本** + 20 字段 schema + **1 条前提假设**。**移出的那部分维护面并未消失，只是没有在最小可用发布里到期**——它记在 `docs/deferred.md`，拿回一项就把这笔账加回来一项。

**落地后的实际账，如实记下**（2026-09-04）：

| | 估 | 实际 | 差在哪 |
| --- | --- | --- | --- |
| skill | 1 | **1** | — |
| hook | 2 | **3** | `fact-priority` 是目标四拿回的那半，分档时它还在 `deferred.md` 里 |
| 脚本 | 4 | **5** | `auth-headers.js` 分档时不存在——它是"规矩指向一个装不上的东西"暴露出来的 |

```text
assert-replay 898 + experience-write 525 + selfcheck 384 + mem0 116 + auth-headers 59
              + recall 185 + fact-priority 102 + deny 32  =  2,301 行
eval 7 份 487 项：297 + 72 + 41 + 30 + 23 + 17 + 7
```

**"2 脚本"变成 5 个**，三个增量各有不可省的理由：`mem0.js` 是写入与检索共用的 REST 契约（各写一份会出现"写得进去但检索不到"且两端不报错）；`selfcheck.js` 是消费方验证插件在他那儿真的生效的手段；`auth-headers.js` 是"不得内联凭据"这条规矩唯一走得通的替代路径。

**这笔账比分档时估的大了约一倍，而且是一路加上来的**——每一项加进来时都能回答"删掉它会发生什么坏事"，但没有一处停下来重算总和。约束 1 说的是"不能再变庞大"，量的正是这个总和。**下一次新增之前，先看这张表。**

**常驻上下文没有跟着涨**：仍然只有 skill 的 description 加 hook 配置。**维护面涨了，两者不是一回事。**

三层治理是本项目唯一一次**主动增重**，理由是有实测支撑：不分层的"抽取即入库"实测约 90% 是废经验，而分层后降到约 5%。**这是全文档唯一有前后对照数据的增项。**（分档后最小可用只带 Review 与 Merge 的 `create`，因此这个 5% 不适用于最小可用阶段，见「写入路径」一节的分档说明。）

新增任何一项前，先回答"删掉它会发生什么具体的坏事"。答不出的不加。

## eval 套件

- **谁跑**：本地 `node evals/run-all.js`；CI 由 `.github/workflows/evals.yml` 在 `plugins/**`、`.claude-plugin/**`、`evals/**` 任一变更时触发
- **阈值**：冒烟集必须 **100%** 通过，方可合入
- **不达标怎样**：拒绝合入该变更

**CI 自动触发这一形态已于 2026-09-04 从 [`docs/deferred.md`](docs/deferred.md) 拿回**——它当初移出的唯一理由是"依赖一个不存在的远端"，而远端已经有了（`git push` 首推，run 33826467231，`windows-latest` 与 `ubuntu-latest` 两条 matrix 均通过）。**在那一刻之前，这个 workflow 声称自己是门禁却一次也没跑过**，正是本项目最怕的那种"写下来就算有了"。

**matrix 跑两个 OS，不是求全**：已知的静默失效点几乎都是 Windows 特有的（MSYS coreutils 自行展开 `~` 与 glob、`echo '单引号 JSON'` 在 `cmd.exe` 下解析失败）。只跑 ubuntu 等于那些用例在门禁里一次也不执行。

**首次运行就量出一个缺口并已修**：`windows-latest` **不自带 `rg`**，于是 rg 的两条断言在 Windows 上静默 skip（416 项 vs 本地 418 项）——**维护者实际用的配置（Windows + rg）在门禁里一次也没跑过**。现在两个 runner 都装 rg。这也说明**"绿了"本身不是判据，得看跑了多少条**。

修完之后两条 matrix 的实测数（run 33826655443）：**`windows-latest` 418 项、0 skip；`ubuntu-latest` 413 项、0 skip**。差的 5 项全部是 `cmd.exe` 特有的断言（`shells` 在 Windows 上是 `['sh','cmd']`、Linux 上只有 `['sh']`，加上那两条 `echo '单引号 JSON'` 的反向对照）——**Linux 上没有 `cmd`，这 5 项本就不该跑**。差额记在这里，免得下次有人看到 413 以为又静默跳过了什么。

**冷启动不等真实任务**——第一批种子直接取自本文档已识别的静默失效模式，今天就能写：

1. ✅ matcher 写错（漏 plugin 前缀 / 无正则字符）时必须被检出为未生效
2. ✅ deny hook 的命令实跑，输出必须是可解析的 deny 决策；并以旧写法作反向对照，证明用例抓得住那个失效
3. ✅ `evidence_cmd` 的执行边界——评审实测过的三条攻击逐条回归，加同族十余条
4. ✅ 畸形输入必须判失败，不得返回"全部通过"；CLI 的 0/1/2 退出码语义
5. ⬜ 裸 `delete_all_memories` 必须被 deny（需连上 mem0）
6. ⬜ mem0 不可达时的降级行为符合预期（见前提假设表）
7. ✅ 注入超过双上限时必须截断

**后来由真实事故补进来的四组**——它们不是设计时想到的，是**踩了才有的**，如实标注来源：

| 组 | 来源 |
| --- | --- |
| 注入路径永不执行命令（`--mode symbols`） | `recall.js` 的 mode 真的被改成过 `full` 并推上远端，而**当时没有任何用例守着它** |
| 仓库里不得出现凭据 | 真实凭据被写进过 `.mcp.json`；拦住它的不是任何机制，是提交前有人看了一眼 |
| 被禁止的做法必须有走得通的替代路径 | 上一条的根因：`headersHelper` 指向的脚本从来没写出来过 |
| 文档提到的路径必须存在 | 同上，最机械也最容易自动核对的那一类 |

**这四组的共同形状是：规则写下了，执行者不在那一层。** 与本项目要防的东西一字不差——只是这次发作在自己身上。

**符号存在性用例必须对着 `evals/fixtures/` 下的受控仓库跑，不得对着本仓库。** 检查搜索整个 cwd，用例里写下的"编造符号"会被自己搜到——自指测量，用例恒不通过。这一条已实际发生过一次。

**经验质量的三指标度量（Recall / Precision / Garbage Rate）、Dedup 层的单独测试、prompt 调优的固定流程，已移入 [`docs/deferred.md`](docs/deferred.md)**——它们要量的是 Review / Dedup 层的输出质量，而度量需要一个固定评测集与人工标注，那需要先有一批真实经验；库为空时这三个数的分母是零。

---

# 前提假设与退路

原有六条假设中的四条已于 **2026-09-03 对活端点实测**（`mcp.mem0.ai` server 版本 `1.29.1`，`api.mem0.ai/v3`），结论见下节。**假设 4（`type: "agent"` hook 可用）同日实测成立**——它只服务于意图偏离检查，随目标一移入 [`docs/deferred.md`](docs/deferred.md)。最小可用只剩一条未实测：

| # | 假设 | 若不成立 | 退路 |
| --- | --- | --- | --- |
| 5 | mem0 不可达时 hook 可静默降级而不阻断会话 | 网络抖动即阻断工作，或闸门与检索一起无声消失 | 检索失败静默跳过并在 transcript 留一行；**写入失败必须硬失败**，不得静默丢弃 |

# 已实测的 mem0 行为（2026-09-03）

**这一节是事实，不是假设。** 每条都由一次真实调用得出。

## `infer: false` 跳过整合，不只是跳过抽取

连写三条语义高度相似的经验（"stop hook 不能同步上报"的三种说法），**三条全部 `event=ADD`，三条都在库里，`replaced_by=null`、`synthesized=false`**。带不带 `immutable` 结果相同。

原先担心的"自动 ADD/UPDATE/DELETE 会吃掉 `occurrences` 与 `status`"**不成立**，因此：

- **`occurrences` / `status` 安全**，甲分支成立
- **不需要 `immutable`**——一度打算给所有 `confirmed` 加上，是多余的

## metadata 是字符串键值模型，不是 JSON

`infer: false` 确实生效（记忆正文原样未被抽取改写），但 metadata 的**序列化**会改形状：

| 写入 | 取回 |
| --- | --- |
| 含引号、反斜杠、换行、中文、shell 元字符的字符串 | **逐字节原样** |
| 多元素数组 `['a','b']` | 原样 |
| 数字 `2` | 字符串 `"2"` |
| 单元素数组 `['x']` | 降级成标量 `"x"` |
| 嵌套对象 `{exit:0,contains:[...]}` | 扁平化成 `["exit.0","contains.…"]` |

**这不是改写，是存储模型。** 最要紧的一条安全：`evidence_cmd` 原样往返，重跑成立。

**因此 schema 只用字符串与多元素数组**，`evidence_digest` 摊平为 `evidence_exit` / `evidence_contains` / `evidence_absent`；读取端一律过 `toArray` 并把退出码转数字——**否则 `"0" !== 0` 会让每一条往返过的经验都判失败，而它们全部会走淘汰。**

## 检索能按自定义 metadata 过滤

`filters: {AND:[{user_id: …}, {metadata:{status:'confirmed'}}]}` 精确命中；`status:'candidate'` 返回 0 条。候选与确认可以同库共存，无需分两个 scope。

`search_memories` 还有 `top_k` / `threshold` / `rerank`——**注入的条数上限可以在服务端截断**，不必取回来再扔；`rerank` 加 150–200ms，要计进 2,000ms 预算。

## 工具名与写入路径

`tools/list` 实得 **11 个工具**，写入与删除类为 `add_memory` / `update_memory` / `delete_memory` / `delete_all_memories` / `delete_entities`——**现行 matcher 覆盖得住**。

`add_memory` 的参数是 `text, messages, user_id, agent_id, app_id, run_id, metadata, infer`——**MCP 直接暴露了 `infer` 与 `metadata`**，先前"MCP 不暴露 infer，所以写入必须走 REST"这个理由不成立。

**但写入仍然走脚本走 REST**，最小可用阶段的理由是两条：**查库必须不可跳过**；**红线过滤与硬失败要有地方落**。

（第三条理由「`add_memory` 没有 `expiration_date`」目前悬空——淘汰已延后。它会在拿回淘汰时重新生效，见 `docs/deferred.md`。）

## `user_id` 与 API key：由引入插件的项目自行配置

至少要传一个 entity ID（`user_id` / `agent_id` / `app_id` / `run_id`）。**团队共享就是大家传同一个 `user_id`。**

**决定（2026-09-04）：两者都由消费方项目配置，本插件不预设默认值、不内置任何 fallback。**

一个默认 `user_id` 不是"开箱即用"——它会让两个不相干的团队共用同一个 scope，把别人的经验注入到你的每一轮提示里。而这恰好是不可让渡约束 4（共享的东西必须有准入）在配置层的形态：**scope 就是共享边界，边界不能有默认值。**

| 取什么 | 环境变量 | 放哪 |
| --- | --- | --- |
| 共享库的 scope | `MEM0_USER_ID` | 消费方项目 `.claude/settings.json` 的 `env`——它不是凭据，**该进版本库**，团队靠它取到同一个值 |
| REST 写入的凭据 | `MEM0_API_KEY` | `.claude/settings.local.json` 的 `env` 或本机密钥库，**不进版本库** |

**两者的归属不同，所以放在不同文件里**：scope 是团队约定，key 是个人凭据。写反了要么共享失效，要么把 key 提交进库。

**缺失时必须硬失败。** 缺 `MEM0_USER_ID` 就随便挑一个，写进去的取不回来；缺 `MEM0_API_KEY` 就跳过写入，用户看到"记下了"而实际什么都没发生。两者都属本插件已点名的**静默失效型**，须由 `selfcheck.js` 覆盖。

**检索与写入必须取到同一对值。** 检索走 MCP（OAuth 或 `headersHelper`），写入走脚本走 REST（读环境变量）——两条路各自取凭据，取到不同的 key 或不同的 `user_id` 时，表现是"写入成功但永远检索不到"，而两端都不报错。**这一致性核对属 `selfcheck.js` 的职责**，是它现有 mem0 连通性检查的自然延伸。

代价不变且已知：共用一个 scope，mem0 侧没有作者归属，`author` 只能自带且不可信。

## 其它数字与字段

- **异步索引延迟约 725 ms**（写入到可查）——甲分支"查库→没查到→写候选"的竞态窗口就是这个量级，候选归并必须考虑
- **`expiration_date` 原样保留**，配合 search 的 `show_expired`（默认 false）
- 条目上还有四个我们没设计过的字段：`categories`、`structured_attributes`、`replaced_by`、`synthesized`。**`replaced_by` 值得留意**——它意味着整合真的发生时是可观测的

# 仍未确认的

**受管环境的 `allowManagedHooksOnly`** 可能整体禁用本项目的 hook，闸门静默失效——团队推广前须确认。

~~**`headersHelper` 的实际行为**——凭据注入依赖它，尚未实测。~~ **已于 2026-09-04 查官方文档确认并落地**，见下「凭据」一节。**本节现在只剩上面那一条。**

## 凭据

**`.mcp.json` 里不放任何凭据**，默认走 OAuth 浏览器登录：

```json
{ "mcpServers": { "mem0": { "type": "http", "url": "https://mcp.mem0.ai/mcp" } } }
```

需要用 API key 的场景（CI、无浏览器），用官方的 **`headersHelper`** 指向一个脚本，由脚本从环境变量或密钥库吐出 header：

```json
"headersHelper": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/auth-headers.js\""
```

**不得使用静态 `headers` 字段**——那等于把 key 提交进版本库。**这一条由 `evals/no-credentials.test.js` 强制**，不是一句劝告。

**这段配置曾经是断的，如实记下（2026-09-04）**：上一版写的是 `…/scripts/auth-headers.sh`，而**那个脚本从来没被写出来过**，且 `.sh` 违反本项目"脚本一律 Node、以 `node <path>` 调用"的既定约定（Windows 上 shebang 与可执行位都不可靠）。后果不是"少个文件"——**一条规则挡住了唯一的替代路径，等于没有规则**：照文档配会失败，于是只剩被禁止的那条静态 `headers` 走得通。实际也确实发生了一次真实凭据被写进 `.mcp.json`。

**契约已查官方文档确认（2026-09-04）**：

| | |
| --- | --- |
| 输出 | **往 stdout 输出一个字符串键值的 JSON 对象**，如 `{"Authorization":"Bearer …"}` |
| 执行 | Claude Code **在 shell 里**跑它，10 秒后放弃 |
| 覆盖关系 | 动态 header 覆盖同名的静态 `headers` |
| `${CLAUDE_PLUGIN_ROOT}` | 在 http 类 server 的 `url` / `headers` / `headersHelper` **三个字段里都会展开** |

**取不到 key 时脚本响亮地失败**（非 0 退出 + stderr），不输出 `{}` 也不输出空 header——前者是静默退回 OAuth 浏览器登录（在 CI 里正是配它要避免的情形），后者是拿一个坏 header 去连、服务端回 401，而用户看到的是"检索不到经验"。**两种都是静默失效。**

**`oauth` 也是受支持字段**（`clientId` / `clientSecret` / `callbackPort`），但**同样不得把 `clientSecret` 写进这个文件**——它是凭据，与 API key 没有区别。官方示例给的是 `clientId` + `callbackPort`，不含 secret。

**`headersHelper` 只管检索那条路。** 它是 MCP 客户端的配置，写入脚本是独立进程、直连 REST，不经过 MCP 客户端，因此对它无效——写入脚本只能读 `MEM0_API_KEY`。**两条路各取各的凭据，一致性由 `selfcheck.js` 核对**（见上「`user_id` 与 API key」）。

## 只能有一个 mem0 MCP 来源

mem0 官方也发布了自己的 Claude Code 插件，它**自带 MCP server**。若与本插件并存，agent 会同时看到两组 mem0 工具，而**它那组的工具名前缀不同（`mcp__plugin_<它的名字>_<server>__*`），本项目的 deny hook 匹配不到**。

**两套并存是最坏的选择**：一组有闸门，一组没有，而 agent 不区分。要么只用本插件的 `.mcp.json`（现行选择），要么改用它的并把 matcher 换成它的前缀。
