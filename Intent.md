# 设计意图

本文件只记录**最上层的抽象意图**。任何具体实现——技术选型、文件结构、接口、判据阈值——都不属于这里，它们在 `DESIGN.md`。

意图的用途是**判断偏离**：当设计或代码演进时，拿它来问「这还是当初要做的那件事吗」。因此本文件应当稳定；它需要修改，说明意图本身变了，那是一次需要明说的决定。

---

## 一、要解决的问题

**AI 每次进入项目都从零开始。**

一次会话里踩坑、纠偏得来的判断，会话结束即归零。下一个人、下一个会话，重新踩一遍。知识留在个别人的脑子里和 git 历史里，不进任何能被下一次会话读到的地方。

**要的不是让 AI 写代码更快，是让它带着项目语境进场。**

## 二、四条目标

以下是原始表述，不改写：

- **意图确立**：进入spec前确立意图，spec下所做的所有变更都不能偏离spec的意图
- **review增强**：将需要通过对话或者写入到文件中的review内容进行增强，让review的内容更直观、易懂
- **经验沉淀**：将vibe以及spec过程中遇到的coding错误记录为经验，让团队共享经验减少重复错误
- **事实准确**：仅将架构、代码作为事实，spec、adr等仅供参考

## 三、贯穿的原则

**每条规则都要挪到产生它的那一层去执行。**

写成条款而执行者不在那一层的规则，会被一致地、有记录地绕过——这不是推测，是量过的。因此本项目不接受"写下来就算有了"的规则：每条规则都要能回答**谁执行它，执行不了会怎样**。

## 四、不可让渡的约束

这四条是**存在理由**层面的，违反其中任何一条，这个项目就没有必要存在：

1. **不能再变庞大。** 前身项目死于此——一份每次会话都要全量读的控制面，一个需求产出十几份工件。新增任何一项之前必须能回答"删掉它会发生什么具体的坏事"。
2. **只增强 superpowers，不接管它。** 不造中央流程文件，不改动它的执行链路。一旦开始定义自己的总谱，就是在重演前身的病因。
3. **经验是参考，不是事实。** 事实只有架构与代码。经验必须携带指向事实的指针，必须可被否证、可被撤回。
4. **共享的东西必须有准入。** 一条错误的经验会以权威姿态进入每个人的上下文，比没有经验更贵。

## 五、明确不做的

列在这里是为了让偏离可判——如果哪天做了其中一项，那是意图变更，不是实现细节：

- **不区分简易路径与完整路径。** 只有一条路。
- **不把经验编译成必须通过的检查**（当前）。它只作检索参考。这一条是有意的取舍，也是已知的收益上限所在。
- **不为"完整"而产出工件。** 每一份工件都要有读它的人和读它的时机。
- **不让产出经验的人同时裁决它。**
- **不把机器无法判定的东西说成判定过了。**

## 六、怎样算偏离

按下面四问检查，任何一问答不上就是偏离：

1. **这项改动服务于四条目标中的哪一条？** 答不出，它不属于本项目。
2. **它引入的规则，执行者是谁？执行不了会怎样？** 答不出，它是一条会被绕过的规则。
3. **它让整体更重了吗？重的那部分换来了什么可指认的好处？** 换不出，就该删。
4. **它有没有把"参考"悄悄变成"事实"，或把"没验证"说成"验证过"？**
